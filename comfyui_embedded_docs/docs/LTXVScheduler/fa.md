# LTXVScheduler

گره LTXVScheduler مقادیر سیگما را برای فرآیندهای نمونه‌برداری سفارشی تولید می‌کند. این گره پارامترهای زمان‌بندی نویز را بر اساس تعداد توکن‌های موجود در latent ورودی محاسبه کرده و برای ایجاد زمان‌بندی نمونه‌برداری، یک تبدیل سیگموئید اعمال می‌کند. گره می‌تواند در صورت نیاز، سیگماهای حاصل را برای مطابقت با یک مقدار پایانی مشخص کشش دهد.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
| --- | --- | --- | --- | --- |
| `steps` | تعداد مراحل نمونه‌برداری (پیش‌فرض: 20) | INT | بله | 1-10000 |
| `max_shift` | حداکثر مقدار شیفت برای محاسبه سیگما (پیش‌فرض: 2.05) | FLOAT | بله | 0.0-100.0 |
| `base_shift` | مقدار شیفت پایه برای محاسبه سیگما (پیش‌فرض: 0.95) | FLOAT | بله | 0.0-100.0 |
| `stretch` | کشش سیگماها برای قرار گرفتن در بازه [terminal, 1] (پیش‌فرض: True) | BOOLEAN | بله | True/False |
| `terminal` | مقدار پایانی سیگماها پس از کشش (پیش‌فرض: 0.1) | FLOAT | بله | 0.0-0.99 |
| `latent` | ورودی اختیاری latent که برای محاسبه تعداد توکن‌ها جهت تنظیم سیگما استفاده می‌شود | LATENT | خیر | - |

**نکته:** پارامتر `latent` اختیاری است. در صورت عدم ارائه، گره از تعداد توکن پیش‌فرض 4096 برای محاسبات استفاده می‌کند.

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
| --- | --- | --- |
| `sigmas` | مقادیر سیگمای تولیدشده برای فرآیند نمونه‌برداری | SIGMAS |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LTXVScheduler/fa.md)

---
**Source fingerprint (SHA-256):** `5b4907e905e27a951c332c400e24023ef089df7a5f4a17b1fc8ba42a41302399`
