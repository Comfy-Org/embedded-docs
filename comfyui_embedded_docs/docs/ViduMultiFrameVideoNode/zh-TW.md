# Vidu 多畫格影片生成

此節點透過在多個關鍵影格之間建立轉場來產生影片。它從初始影像開始，依序動畫播放一系列使用者定義的結束影像與提示，最終輸出單一影片檔案。

## 輸入

### 通用輸入

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `model` | 用於影片生成的 Vidu 模型。 | COMBO | 是 | "viduq2-pro"<br>"viduq2-turbo" |
| `start_image` | 起始影格影像。長寬比必須介於 1:4 與 4:1 之間。 | IMAGE | 是 | 長寬比 1:4 至 4:1 |
| `seed` | 用於隨機數產生的種子值，以確保結果可重現（預設：1）。 | INT | 是 | 0 到 2147483647 |
| `resolution` | 輸出影片的解析度。 | COMBO | 是 | "720p"<br>"1080p" |
| `frames` | 關鍵影格轉場的數量（2-9）。選擇一個值將動態顯示每個影格所需的輸入。 | DYNAMIC_COMBO | 是 | "2"<br>"3"<br>"4"<br>"5"<br>"6"<br>"7"<br>"8"<br>"9" |

### 影格輸入（所有影格數量選項共用）

當 `frames` 設定為某個數字時，以下三個輸入會針對從 1 到該數字的每個影格 `i` 顯示。例如，選擇「3」會新增 `prompt1` / `end_image1` / `duration1`、`prompt2` / `end_image2` / `duration2` 和 `prompt3` / `end_image3` / `duration3`。

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `prompt{i}` | 第 {i} 個影格轉場的文字提示。多行文字欄位。最多 2000 字元。 | STRING | 是 | 最多 2000 字元 |
| `end_image{i}` | 第 {i} 個片段的結束影格影像。長寬比必須介於 1:4 與 4:1 之間。 | IMAGE | 是 | 長寬比 1:4 至 4:1 |
| `duration{i}` | 第 {i} 個片段的持續時間（以秒為單位，預設：4）。 | INT | 是 | 2 到 7 |

**注意：**

- 所有輸入皆為必填。`seed` 有預設值，但仍為必填輸入。
- `start_image` 與每個 `end_image{i}` 的長寬比必須介於 1:4 與 4:1 之間。
- 每個 `prompt{i}` 的最大長度為 2000 字元。
- 每個 `duration{i}` 必須介於 2 到 7 秒之間。

## 輸出

| 輸出名 | 說明 | 資料類型 |
|-----------|-------------|-----------|
| `output` | 生成的影片檔案，包含所有動畫轉場。 | VIDEO |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ViduMultiFrameVideoNode/zh-TW.md)

---
**Source fingerprint (SHA-256):** `ad877532ba27444938b7b2e4634ac7f8a47db0f7fb53967d874ad38b44336dcf`
