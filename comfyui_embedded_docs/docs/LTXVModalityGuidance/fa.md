# LTXVModalityGuidance

این گره راهنماییِ متقاطع-حالتی (صوتی-تصویری) را بر روی مدل LTXV-AV اعمال می‌کند. در طول نمونه‌برداری، به‌ازای هر گام یک گذر روبه‌جلو اضافی اجرا می‌کند که در آن اتصالات توجه متقاطع از صدا به تصویر و از تصویر به صدا غیرفعال شده‌اند و سپس نتیجه را به سمت پیش‌بینیِ جفت‌شده سوق می‌دهد. این کار همگام‌سازی صوتی-تصویری، مانند هم‌زمانی حرکت لب را تقویت می‌کند. مقدار پیش‌فرض مرجع برای `modality_scale` برابر با 3.0 است؛ تنظیم آن به 1.0 گذر اضافی را غیرفعال می‌کند.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
|---------|---------|----------|--------|--------|
| `model` | مدل پایه‌ای که راهنمایی حالتی به آن اعمال می‌شود. به‌صورت داخلی کلون می‌شود و مدل اصلی بدون تغییر باقی می‌ماند. | MODEL | بله | - |
| `modality_scale` | شدت راهنمایی جفت‌شدگی صوتی-تصویری. پیش‌فرض 3.0 است. برای غیرفعال‌سازی گذر روبه‌جلو اضافی، آن را روی 1.0 تنظیم کنید. | FLOAT | بله | 1.0 تا 100.0 (default: 3.0) |
| `start_percent` | نقطه‌ای در فرایند نمونه‌برداری، به‌صورت درصد از 0.0 تا 1.0، که در آن راهنمایی حالتی آغاز می‌شود. پیش‌فرض 0.0 است. | FLOAT | بله | 0.0 تا 1.0 (default: 0.0) |
| `end_percent` | نقطه‌ای در فرایند نمونه‌برداری، به‌صورت درصد از 0.0 تا 1.0، که در آن راهنمایی حالتی پایان می‌یابد. پیش‌فرض 1.0 است. | FLOAT | بله | 0.0 تا 1.0 (default: 1.0) |

راهنمایی فقط برای گام‌های نمونه‌برداری اعمال می‌شود که مقادیر سیگمای آن‌ها در بازه‌ای که توسط `start_percent` و `end_percent` تعریف شده، قرار می‌گیرند. خارج از این بازه، گره نتیجهٔ نویززدایی‌شده را بدون تغییر برمی‌گرداند. مقدار `modality_scale` برابر با 1.0 نیز گذر روبه‌جلو اضافی را کاملاً غیرفعال می‌کند.

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
|-----------|---------|----------|
| `model` | مدل کلون‌شده با تابع راهنمایی پس از CFG متصل‌شده. این مدل اصلاح‌شده در طول نمونه‌برداری، راهنمایی حالتی را اعمال می‌کند. | MODEL |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LTXVModalityGuidance/fa.md)

---
**Source fingerprint (SHA-256):** `038be607c42e626a8a8f5fe336ee466d0847d43835edb71e20ff38f668069cfb`
