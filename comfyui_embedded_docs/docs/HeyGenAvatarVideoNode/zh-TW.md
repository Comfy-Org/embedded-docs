# HeyGen 虛擬分身影片

從 HeyGen 虛擬化身生成談話主持人影片。此節點使用 HeyGen 的渲染引擎，建立 AI 虛擬化身朗讀您提供的文字或對您的音訊進行唇形同步的影片。

## 輸入

### 通用輸入

| 參數 | 描述 | 資料類型 | 必要 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `引擎` | 渲染引擎；每個選項僅列出支援該引擎的虛擬化身。「auto」提供所有虛擬化身，並自動選擇其最佳引擎（偏好 Avatar IV）。Avatar V 保真度最高，Avatar III 最經濟實惠。 | DYNAMIC_COMBO | 是 | `"auto"`<br>`"avatar_iv"`<br>`"avatar_iii"`<br>`"avatar_v"` |
| `自訂分身 ID` | 選用的 HeyGen 虛擬化身外觀 ID。設定後會覆寫上方選擇的虛擬化身。可使用 HeyGen 的 3000 多種公開外觀（或您的私人虛擬化身）中的任何一種。預設值：`""`。 | STRING | 否 |  |
| `語音` | 使用文字指令碼（HeyGen 文字轉語音）或您自己的音訊來驅動虛擬化身。顯示名稱：「speech source」。 | DYNAMIC_COMBO | 是 | `"script"`<br>`"audio"` |
| `解析度` | 輸出影片解析度。預設值：`"1080p"`。 | COMBO | 否 | `"720p"`<br>`"1080p"` |
| `長寬比` | 輸出長寬比。「auto」會跟隨虛擬化身的原始素材。預設值：`"auto"`。 | COMBO | 否 | `"auto"`<br>`"16:9"`<br>`"9:16"`<br>`"1:1"`<br>`"4:5"`<br>`"5:4"` |
| `背景顏色` | 選用的純色背景顏色，以十六進位色碼表示（例如 `"#00ff00"`）。留空則使用虛擬化身原本的背景。若提供，數值必須以 `#` 開頭。預設值：`""`。 | STRING | 否 |  |
| `種子` | 不會傳送至 HeyGen；變更此值以強制重新執行。預設值：`42`。 | INT | 否 | 最小值：0<br>最大值：2147483647 |

### `auto` 輸入

當 `engine` 為 `"auto"` 時，可使用下列子參數：

| 參數 | 描述 | 資料類型 | 必要 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 用於呈現影片的虛擬化身外觀（從 HeyGen 公開資料庫中精選）。系統會自動選擇該外觀所支援的最佳引擎。 | COMBO | 是 | 多個選項可用 |

### `avatar_iv` 輸入

當 `engine` 為 `"avatar_iv"` 時，可使用下列子參數：

| 參數 | 描述 | 資料類型 | 必要 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 支援 Avatar IV 引擎的虛擬化身外觀。 | COMBO | 是 | 多個選項可用 |

### `avatar_iii` 輸入

當 `engine` 為 `"avatar_iii"` 時，可使用下列子參數：

| 參數 | 描述 | 資料類型 | 必要 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 支援 Avatar III 引擎的虛擬化身外觀。 | COMBO | 是 | 多個選項可用 |

### `avatar_v` 輸入

當 `engine` 為 `"avatar_v"` 時，可使用下列子參數：

| 參數 | 描述 | 資料類型 | 必要 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 支援 Avatar V 引擎的虛擬化身外觀。 | COMBO | 是 | 多個選項可用 |

### `script` 輸入

當 `speech` 為 `"script"` 時，可使用下列子參數：

| 參數 | 描述 | 資料類型 | 必要 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `text` | 虛擬化身要朗讀的文字（最多 5000 個字元）。生成的語音長度必須至少 1 秒。預設值：`""`。 | STRING | 是 | 最小值：1 個字元<br>最大值：5000 個字元 |
| `voice` | 指令碼使用的語音。預設選項使用 HeyGen 指派給虛擬化身的語音。若已設定 `custom_voice_id`，則此項會被忽略。 | COMBO | 是 | `"(avatar's default voice)"`<br>多個一般語音選項可用 |
| `custom_voice_id` | 選用的 HeyGen 語音 ID。設定後會覆寫上方選擇的語音。可使用 HeyGen 語音庫（2000 多種）中的任何語音。預設值：`""`。 | STRING | 否 |  |
| `voice_speed` | 語速倍率。預設值：`1.0`。 | FLOAT | 否 | 最小值：0.5<br>最大值：1.5<br>步階：0.05 |

### `audio` 輸入

當 `speech` 為 `"audio"` 時，可使用下列子參數：

| 參數 | 描述 | 資料類型 | 必要 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `audio` | 供虛擬化身進行唇形同步的音訊，最多 10 分鐘。 | AUDIO | 是 |  |

注意：`engine` 和 `speech` 是選擇器，會根據所選的值顯示不同的子參數。`speech` 選擇器有兩種互斥的模式：在 `"script"` 模式下，`text` 為必填；若提供了 `custom_voice_id`，則會覆寫 `voice`。在 `"audio"` 模式下，虛擬化身會對提供的音訊片段進行唇形同步。`background_color` 若提供，則必須是以 `#` 開頭的十六進位色碼。當設定了 `custom_avatar_id` 時，它會覆寫 `avatar` 的選擇，且所選的 `engine` 必須受該虛擬化身外觀支援；否則會產生錯誤，除非 `engine` 為 `"auto"`。

## 輸出

| 輸出名 | 描述 | 資料類型 |
|-------------|-------------|-----------|
| `VIDEO` | 生成的虛擬化身主持人影片。 | VIDEO |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/HeyGenAvatarVideoNode/zh-TW.md)

---
**Source fingerprint (SHA-256):** `86dc799d3a8cf2666449b0d422853b12feffb81ce002f84594f9b925d58b522a`
