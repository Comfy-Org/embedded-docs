# HeyGen 虛擬分身影片

從 HeyGen 虛擬人像產生說話主持人影片。此節點會使用 HeyGen 的渲染引擎，建立一段 AI 虛擬人像朗讀您所提供文字，或對您自己的音訊進行唇形同步的影片。

## 輸入

### 通用輸入

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `引擎` | 渲染引擎；每個選項只會列出支援該引擎的虛擬人像。`"auto"` 會提供所有虛擬人像，並自動挑選其最佳引擎（優先使用 Avatar IV）。Avatar V 的擬真度最高，Avatar III 最經濟實惠。 | DYNAMIC_COMBO | 是 | `"auto"`<br>`"avatar_iv"`<br>`"avatar_iii"`<br>`"avatar_v"` |
| `自訂分身 ID` | 選用的 HeyGen 虛擬人像外觀 ID。設定後，會覆寫上方選取的虛擬人像。可使用 HeyGen 超過 3000 個公開外觀中的任何一個（或您的私人虛擬人像）。預設值：`""`。 | STRING | 否 |  |
| `語音` | 使用文字腳本（HeyGen 文字轉語音）或您自己的音訊來驅動虛擬人像。顯示名稱："speech source"。 | DYNAMIC_COMBO | 是 | `"script"`<br>`"audio"` |
| `解析度` | 輸出影片解析度。預設值：`"1080p"`。 | COMBO | 否 | `"720p"`<br>`"1080p"` |
| `長寬比` | 輸出長寬比。`"auto"` 會跟隨虛擬人像的來源影片畫面。預設值：`"auto"`。 | COMBO | 否 | `"auto"`<br>`"16:9"`<br>`"9:16"`<br>`"1:1"`<br>`"4:5"`<br>`"5:4"` |
| `背景顏色` | 選用的純色背景色，以十六進位色碼表示（例如 `"#00ff00"`）。留空則使用虛擬人像自有的背景。若提供此值，必須以 `#` 開頭。預設值：`""`。 | STRING | 否 |  |
| `種子` | 不會傳送給 HeyGen；變更此值可強制重新執行。預設值：`42`。 | INT | 否 | 最小：0<br>最大：2147483647 |

### `auto` 輸入

當 `engine` 為 `"auto"` 時，可使用下列子參數：

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 用於呈現影片的虛擬人像外觀（從 HeyGen 的公開庫中精選）。系統會自動選擇該外觀支援的最佳引擎。 | COMBO | 是 | 提供多個選項 |

### `avatar_iv` 輸入

當 `engine` 為 `"avatar_iv"` 時，可使用下列子參數：

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 支援 Avatar IV 引擎的虛擬人像外觀。 | COMBO | 是 | 提供多個選項 |

### `avatar_iii` 輸入

當 `engine` 為 `"avatar_iii"` 時，可使用下列子參數：

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 支援 Avatar III 引擎的虛擬人像外觀。 | COMBO | 是 | 提供多個選項 |

### `avatar_v` 輸入

當 `engine` 為 `"avatar_v"` 時，可使用下列子參數：

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 支援 Avatar V 引擎的虛擬人像外觀。 | COMBO | 是 | 提供多個選項 |

### `script` 輸入

當 `speech` 為 `"script"` 時，可使用下列子參數：

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `text` | 供虛擬人像朗讀的文字（最多 5000 個字元）。產生的語音長度必須至少 1 秒。預設值：`""`。 | STRING | 是 | 最小：1 個字元<br>最大：5000 個字元 |
| `voice` | 腳本使用的語音。預設選項會使用 HeyGen 指派給該虛擬人像的語音。若設定了 `custom_voice_id`，則會忽略此項。 | COMBO | 是 | `"(avatar's default voice)"`<br>提供多個通用語音選項 |
| `custom_voice_id` | 選用的 HeyGen 語音 ID。設定後，會覆寫上方選取的語音。可使用 HeyGen 資料庫中超過 2000 種語音中的任何一種。預設值：`""`。 | STRING | 否 |  |
| `voice_speed` | 語速倍率。預設值：`1.0`。 | FLOAT | 否 | 最小：0.5<br>最大：1.5<br>步進：0.05 |

### `audio` 輸入

當 `speech` 為 `"audio"` 時，可使用下列子參數：

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `audio` | 供虛擬人像進行唇形同步的音訊，最長 10 分鐘。 | AUDIO | 是 |  |

注意：`engine` 和 `speech` 是選擇器，會根據所選值顯示不同的子參數。`speech` 選擇器有兩種互斥模式：在 `"script"` 模式下，必須提供 `text`；若提供了 `custom_voice_id`，它會覆寫 `voice`。在 `"audio"` 模式下，虛擬人像會對提供的音訊片段進行唇形同步。提供 `background_color` 時，它必須是以 `#` 開頭的十六進位色碼。設定 `custom_avatar_id` 時，它會覆寫 `avatar` 選擇，且所選的 `engine` 必須受該虛擬人像外觀支援；否則會引發錯誤，除非 `engine` 為 `"auto"`。

## 輸出

| 輸出名稱 | 說明 | 資料類型 |
|-------------|-------------|-----------|
| `VIDEO` | 產生的虛擬人像主持人影片。 | VIDEO |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/HeyGenAvatarVideoNode/zh-TW.md)

---
**Source fingerprint (SHA-256):** `86dc799d3a8cf2666449b0d422853b12feffb81ce002f84594f9b925d58b522a`
