# HeyGen 虛擬分身影片

從 HeyGen 虛擬人物產生說話型簡報影片。此節點使用 HeyGen 的渲染引擎，建立 AI 虛擬人物朗讀您提供的文字或對您的音訊進行唇形同步的影片。

## 輸入

### 通用輸入

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `引擎` | 渲染引擎；每個選項只會列出支援該引擎的虛擬人物。「auto」提供所有虛擬人物，並自動挑選最適合的引擎（偏好 Avatar IV）。Avatar V 畫質最高，Avatar III 最經濟實惠。 | DYNAMIC_COMBO | 是 | `"auto"`<br>`"avatar_iv"`<br>`"avatar_iii"`<br>`"avatar_v"` |
| `自訂分身 ID` | 選用的 HeyGen 虛擬人物外觀 ID。設定後會覆寫上方選取的虛擬人物。可使用 HeyGen 公開資料庫中 3000 多種外觀（或您的私人虛擬人物）。預設值：空字串。 | STRING | 否 |  |
| `語音` | 使用文字腳本（HeyGen 文字轉語音）或您自己的音訊驅動虛擬人物。 | DYNAMIC_COMBO | 是 | `"script"`<br>`"audio"` |
| `解析度` | 輸出影片解析度。預設值：`"1080p"`。 | COMBO | 否 | `"720p"`<br>`"1080p"` |
| `長寬比` | 輸出畫面比例。「auto」會沿用虛擬人物的原始素材比例。預設值：`"auto"`。 | COMBO | 否 | `"auto"`<br>`"16:9"`<br>`"9:16"`<br>`"1:1"`<br>`"4:5"`<br>`"5:4"` |
| `背景顏色` | 選用的純色背景十六進位色碼（例如 `"#00ff00"`）。留空則使用虛擬人物原有的背景。若提供，數值必須以 `#` 開頭。預設值：空字串。 | STRING | 否 |  |
| `種子` | 不會傳送至 HeyGen；變更此值可強制重新執行。預設值：`42`。 | INT | 否 | 最小值：0<br>最大值：2147483647 |

### `auto` 輸入

當 `engine` 為 `"auto"` 時，可使用下列子參數：

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 用於呈現影片的虛擬人物外觀（精選自 HeyGen 公開資料庫）。系統會自動選擇該外觀所支援的最佳引擎。 | COMBO | 是 | 多個可用選項 |

### `avatar_iv` 輸入

當 `engine` 為 `"avatar_iv"` 時，可使用下列子參數：

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 支援 Avatar IV 引擎的虛擬人物外觀。 | COMBO | 是 | 多個可用選項 |

### `avatar_iii` 輸入

當 `engine` 為 `"avatar_iii"` 時，可使用下列子參數：

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 支援 Avatar III 引擎的虛擬人物外觀。 | COMBO | 是 | 多個可用選項 |

### `avatar_v` 輸入

當 `engine` 為 `"avatar_v"` 時，可使用下列子參數：

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | 支援 Avatar V 引擎的虛擬人物外觀。 | COMBO | 是 | 多個可用選項 |

### `script` 輸入

當 `speech` 為 `"script"` 時，可使用下列子參數：

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `text` | 虛擬人物要朗讀的文字（最多 5000 個字元）。產生的語音長度必須至少 1 秒。預設值：空字串。 | STRING | 是 | 最小值：1 個字元<br>最大值：5000 個字元 |
| `voice` | 腳本使用的語音。預設選項使用 HeyGen 指派給虛擬人物的語音。 | COMBO | 是 | `"(avatar's default voice)"`<br>多個通用語音選項 |
| `custom_voice_id` | 選用的 HeyGen 語音 ID。設定後會覆寫上方選取的語音。可使用 HeyGen 語音資料庫（2000 多種）中的任何語音。預設值：空字串。 | STRING | 否 |  |
| `voice_speed` | 語速倍率。預設值：`1.0`。 | FLOAT | 否 | 最小值：0.5<br>最大值：1.5<br>步長：0.05 |

### `audio` 輸入

當 `speech` 為 `"audio"` 時，可使用下列子參數：

| 參数 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `audio` | 供虛擬人物進行唇形同步的音訊，最長 10 分鐘。 | AUDIO | 是 |  |

注意：`speech` 是來源選擇器，具有兩種互斥模式。在 `"script"` 模式中，`text` 為必填（1 至 5000 個字元）；若提供 `custom_voice_id`，則會覆寫 `voice`。在 `"audio"` 模式中，虛擬人物會對提供的音訊片段進行唇形同步。`background_color` 若提供，必須是以 `#` 開頭的十六進位色碼。設定 `custom_avatar_id` 時，會覆寫 `avatar` 選取項目，且所選的 `engine` 必須為該虛擬人物外觀所支援；否則會產生錯誤（除非 `engine` 為 `"auto"`）。

## 輸出

| 輸出名 | 說明 | 資料類型 |
|-------------|-------------|-----------|
| `VIDEO` | 產生的虛擬人物簡報影片。 | VIDEO |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/HeyGenAvatarVideoNode/zh-TW.md)

---
**Source fingerprint (SHA-256):** `009bc72b841ca273af83fe6f80fb24d4b11c2efd96c011795b1ff1cf8e66ee61`
