# HeyGen アバター動画

HeyGenアバターからトーキングプレゼンタービデオを生成します。このノードは、HeyGenのレンダリングエンジンを使用して、指定したテキストを話すAIアバター、または独自のオーディオにリップシンクするAIアバターのビデオを作成します。

## 入力

### 共通入力

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `エンジン` | レンダリングエンジンです。各選択肢には、それをサポートするアバターのみが表示されます。「auto」はすべてのアバターを提供し、最適なエンジンを自動選択します（Avatar IV優先）。Avatar Vは最高忠実度、Avatar IIIは最も手頃な価格です。 | DYNAMIC_COMBO | はい | `"auto"`<br>`"avatar_iv"`<br>`"avatar_iii"`<br>`"avatar_v"` |
| `カスタムアバターID` | オプションのHeyGenアバタールックIDです。設定すると、上で選択したアバターを上書きします。HeyGenの3000以上の公開ルック（または非公開アバター）を使用できます。デフォルト: 空文字列。 | STRING | いいえ |  |
| `スピーチ` | テキストスクリプト（HeyGenテキスト読み上げ）または独自のオーディオでアバターを駆動します。 | DYNAMIC_COMBO | はい | `"script"`<br>`"audio"` |
| `解像度` | 出力ビデオ解像度です。デフォルト: `"1080p"`。 | COMBO | いいえ | `"720p"`<br>`"1080p"` |
| `アスペクト比` | 出力アスペクト比です。「auto」はアバターの元の映像に従います。デフォルト: `"auto"`。 | COMBO | いいえ | `"auto"`<br>`"16:9"`<br>`"9:16"`<br>`"1:1"`<br>`"4:5"`<br>`"5:4"` |
| `背景色` | オプションの無地の背景色を16進コードで指定します（例: `"#00ff00"`）。アバターの元の背景を使用する場合は空のままにします。指定する場合、値は`#`で始まる必要があります。デフォルト: 空文字列。 | STRING | いいえ |  |
| `シード` | HeyGenには送信されません。再実行を強制するために変更します。デフォルト: `42`。 | INT | いいえ | 最小: 0<br>最大: 2147483647 |

### `auto` 入力

`engine`が`"auto"`の場合、以下のサブパラメータが使用できます。

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | ビデオのプレゼンターとなるアバタールックです（HeyGenの公開ライブラリから厳選）。そのルックがサポートする最適なエンジンが自動的に選択されます。 | COMBO | はい | 利用可能な複数のオプション |

### `avatar_iv` 入力

`engine`が`"avatar_iv"`の場合、以下のサブパラメータが使用できます。

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | Avatar IVエンジンをサポートするアバタールックです。 | COMBO | はい | 利用可能な複数のオプション |

### `avatar_iii` 入力

`engine`が`"avatar_iii"`の場合、以下のサブパラメータが使用できます。

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | Avatar IIIエンジンをサポートするアバタールックです。 | COMBO | はい | 利用可能な複数のオプション |

### `avatar_v` 入力

`engine`が`"avatar_v"`の場合、以下のサブパラメータが使用できます。

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `avatar` | Avatar Vエンジンをサポートするアバタールックです。 | COMBO | はい | 利用可能な複数のオプション |

### `script` 入力

`speech`が`"script"`の場合、以下のサブパラメータが使用できます。

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `text` | アバターが話すテキストです（最大5000文字）。生成される音声の長さは1秒以上である必要があります。デフォルト: 空文字列。 | STRING | はい | 最小: 1文字<br>最大: 5000文字 |
| `voice` | スクリプト用の音声です。デフォルトのオプションは、HeyGenがアバターに割り当てた音声を使用します。 | COMBO | はい | `"(avatar's default voice)"`<br>利用可能な複数の一般的な音声オプション |
| `custom_voice_id` | オプションのHeyGen音声IDです。設定すると、上で選択した音声を上書きします。HeyGenのライブラリ（2000以上）の任意の音声を使用できます。デフォルト: 空文字列。 | STRING | いいえ |  |
| `voice_speed` | 音声速度の倍率です。デフォルト: `1.0`。 | FLOAT | いいえ | 最小: 0.5<br>最大: 1.5<br>ステップ: 0.05 |

### `audio` 入力

`speech`が`"audio"`の場合、以下のサブパラメータが使用できます。

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `audio` | アバターがリップシンクするためのオーディオです。最大10分。 | AUDIO | はい |  |

注: `speech`は、相互に排他的な2つのモードを持つソースセレクターです。`"script"`モードでは、`text`が必須です（1～5000文字）。`custom_voice_id`が指定されている場合は、`voice`を上書きします。`"audio"`モードでは、アバターは指定されたオーディオクリップにリップシンクします。`background_color`は、指定する場合、`#`で始まる16進カラーコードである必要があります。`custom_avatar_id`が設定されている場合、`avatar`の選択を上書きし、選択した`engine`がそのアバタールックでサポートされている必要があります。そうでない場合はエラーが発生します（`engine`が`"auto"`の場合を除く）。

## 出力

| 出力名 | 説明 | データ型 |
|-------------|-------------|-----------|
| `VIDEO` | 生成されたアバタープレゼンタービデオ。 | VIDEO |

> このドキュメントは AI によって生成されました。エラーを見つけた場合や改善のご提案がある場合は、ぜひ貢献してください！ [GitHub で編集](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/HeyGenAvatarVideoNode/ja.md)

---
**Source fingerprint (SHA-256):** `009bc72b841ca273af83fe6f80fb24d4b11c2efd96c011795b1ff1cf8e66ee61`
