> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [Edit on GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/StabilityStableImageSD_3_5Node/zh-TW.md)

此節點使用 Stability AI 的 Stable Diffusion 3.5 模型同步生成影像。它能根據文字提示建立影像，並在提供輸入影像時修改現有影像。該節點支援多種長寬比和風格預設，以自訂輸出結果。

## 輸入參數

| 參數名稱 | 資料類型 | 必填 | 數值範圍 | 描述 |
|-----------|-----------|----------|-------|-------------|
| `prompt` | STRING | 是 | - | 您希望在輸出影像中看到的內容。一個強而有力、描述清晰的提示，能明確定義元素、色彩和主體，將帶來更好的結果。（預設值：空字串） |
| `model` | COMBO | 是 | 提供多個選項 | 用於生成的 Stable Diffusion 3.5 模型。 |
| `長寬比` | COMBO | 是 | 提供多個選項 | 生成影像的長寬比。（預設值：1:1 比例） |
| `style_preset` | COMBO | 否 | 提供多個選項 | 生成影像的選用期望風格。 |
| `cfg_scale` | FLOAT | 是 | 1.0 至 10.0 | 擴散過程遵循提示文字的嚴格程度（數值越高，影像越接近您的提示）。（預設值：4.0） |
| `種子` | INT | 是 | 0 至 4294967294 | 用於建立雜訊的隨機種子。（預設值：0） |
| `影像` | IMAGE | 否 | - | 用於圖像到圖像生成的選用輸入影像。 |
| `負向提示詞` | STRING | 否 | - | 您不希望出現在輸出影像中的關鍵字。這是一項進階功能。（預設值：空字串） |
| `image_denoise` | FLOAT | 否 | 0.0 至 1.0 | 輸入影像的去噪程度；0.0 會產生與輸入完全相同的影像，1.0 則如同完全未提供影像。（預設值：0.5） |

**注意：** 當提供 `image` 時，節點會切換到圖像到圖像生成模式，且 `aspect_ratio` 參數會自動從輸入影像中確定。當未提供 `image` 時，`image_denoise` 參數將被忽略。

## 輸出結果

| 輸出名稱 | 資料類型 | 描述 |
|-------------|-----------|-------------|
| `影像` | IMAGE | 生成或修改後的影像。 |