# Преобразование числа

Узел Number Convert преобразует различные типы входных данных в числовые значения. Он принимает один вход типа целое число, число с плавающей запятой, строка или логическое значение и создаёт два выхода: число с плавающей запятой и целое число. Это полезно для преобразования текстовых или логических значений в формат, который может использоваться другими математическими или обрабатывающими узлами в вашем рабочем процессе.

## Входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `значение` | Значение, которое нужно преобразовать в числовые выходы. Принимает целое число, число с плавающей запятой, текстовую строку или логическое значение true/false. | INT, FLOAT, STRING, BOOLEAN | Да | N/A |

**Примечание:** Если входное значение — строка, она не должна быть пустой и должна содержать допустимое представление числа (например, `"123"`, `"3.14"`). Узел выдаст ошибку для пустых строк, текста, который не удаётся разобрать как число, или значений, не являющихся конечными (например, `"inf"` или `"nan"`). Для логических входов `true` преобразуется в 1.0 (FLOAT) и 1 (INT), а `false` — в 0.0 (FLOAT) и 0 (INT). Для входов типа float и для строк, содержащих десятичное число, целочисленный выход получается путём отбрасывания дробной части.

## Выходы

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `FLOAT` | Входное значение, преобразованное в число с плавающей запятой. | FLOAT |
| `INT` | Входное значение, преобразованное в целое число. Для входов типа float и десятичных строк выполняется усечение. | INT |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ComfyNumberConvert/ru.md)

---
**Source fingerprint (SHA-256):** `d6a774bf7c3ad9482b2275adc6ccc9e47c82b1f35f11c5a241b00efd29526f94`
