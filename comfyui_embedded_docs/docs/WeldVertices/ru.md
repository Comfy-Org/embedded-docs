# WeldVertices

Weld Vertices объединяет совпадающие вершины в 3D-сетке, поэтому грани, которые ранее имели отдельные угловые точки, в итоге используют общие вершины. Он группирует близлежащие вершины с помощью квантования сетки с допуском, основанным на ограничивающем объеме сетки, и усредняет цвета вершин для каждой объединенной группы. Это полезно, когда сетка поступает необъединенной, то есть каждая грань имеет свои собственные вершины и не имеет общих ребер.

## Входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
|-----------|-------------|-----------|----------|-------|
| `сетка` | Входная 3D-сетка, чьи совпадающие вершины будут объединены. | MESH | Да | - |
| `epsilon_rel` | Допуск сварки (доля диагонали ограничивающего объема). 1e-5 для дедупликации с плавающей точкой; 1e-3 для визуально близких, но различных вершин. По умолчанию: 1e-5. | FLOAT | Да | 0.0 to unlimited |
| `epsilon_abs` | Абсолютный допуск сварки (переопределяет epsilon_rel, когда > 0). По умолчанию: 0.0. | FLOAT | Да | 0.0 to unlimited |

Примечание: Когда `epsilon_abs` больше 0, он имеет приоритет над `epsilon_rel`, и относительный допуск игнорируется. Когда `epsilon_abs` равен 0, используется относительный допуск `epsilon_rel`.

## Выходы

| Имя выхода | Описание | Тип данных |
|-------------|-------------|-----------|
| `сетка` | Сваренная сетка с объединенными вершинами, обновленными индексами граней и усредненными цветами вершин (если входная сетка имела цвета). | MESH |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/WeldVertices/ru.md)

---
**Source fingerprint (SHA-256):** `f8779e764b344de651b8459f6e4c28773509d9596a98fd164dc7044278856435`
