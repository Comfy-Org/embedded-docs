> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ReveImageEditNode/fa.md)

## ورودی‌ها

| پارامتر | نوع داده | الزامی | محدوده | توضیحات |
|---------|----------|--------|--------|---------|
| `تصویر` | IMAGE | بله | - | تصویری که باید ویرایش شود. |
| `دستور ویرایش` | STRING | بله | - | توضیح متنی نحوه ویرایش تصویر. حداکثر ۲۵۶۰ کاراکتر. |
| `مدل` | MODEL | بله | `"reve-edit@20250915"`<br>`"reve-edit-fast@20251030"` | نسخه مدل مورد استفاده برای ویرایش. |
| `model.aspect_ratio` | COMBO | خیر | `"auto"`<br>`"16:9"`<br>`"9:16"`<br>`"3:2"`<br>`"2:3"`<br>`"4:3"`<br>`"3:4"`<br>`"1:1"` | نسبت ابعاد تصویر ویرایش‌شده. هنگام تنظیم روی "auto"، نسبت ابعاد به‌طور خودکار تعیین می‌شود. |
| `model.test_time_scaling` | FLOAT | خیر | - | ضریب مقیاس‌گذاری زمان آزمون برای مدل. مقادیر بالاتر ممکن است کیفیت را بهبود بخشد اما زمان پردازش را افزایش می‌دهد. |
| `افزایش وضوح` | COMBO | خیر | `"disabled"`<br>`"enabled"` | کنترل می‌کند که آیا تصویر تولیدشده بزرگ‌نمایی شود یا خیر. |
| `upscale.upscale_factor` | FLOAT | خیر | - | ضریب بزرگ‌نمایی تصویر هنگام فعال بودن بزرگ‌نمایی. |
| `حذف پس‌زمینه` | BOOLEAN | خیر | - | کنترل می‌کند که آیا پس‌زمینه از تصویر تولیدشده حذف شود یا خیر. |
| `seed` | INT | خیر | ۰ تا ۲۱۴۷۴۸۳۶۴۷ | دانه (seed) کنترل می‌کند که آیا گره باید دوباره اجرا شود؛ نتایج صرف‌نظر از دانه، قطعی نیستند. (پیش‌فرض: ۰) |

**توجه:** پارامتر `upscale.upscale_factor` تنها زمانی مرتبط است که پارامتر `upscale` روی `"enabled"` تنظیم شده باشد.

## خروجی‌ها

| نام خروجی | نوع داده | توضیحات |
|-----------|----------|---------|
| `تصویر` | IMAGE | تصویر ویرایش‌شده بر اساس دستورالعمل تولیدشده. |

---
**Source fingerprint (SHA-256):** `0a9504ae5e8b7216d309fe3ba95c014da32eadbf11cfc5701247ba5973dd98be`
