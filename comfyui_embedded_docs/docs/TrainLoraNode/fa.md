> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/TrainLoraNode/fa.md)

گره TrainLoraNode یک مدل LoRA (انطباق رتبه-پایین) را بر روی یک مدل انتشار با استفاده از لیتنت‌ها و داده‌های شرطی‌سازی ارائه‌شده ایجاد و آموزش می‌دهد. این گره به شما امکان می‌دهد یک مدل را با پارامترهای آموزشی سفارشی، بهینه‌سازها و توابع خطا تنظیم کنید. خروجی گره شامل وزن‌های LoRA آموزش‌دیده، یک نقشه تاریخچه خطا و تعداد کل مراحل آموزشی تکمیل‌شده است.

## ورودی‌ها

| پارامتر | نوع داده | الزامی | محدوده | توضیحات |
|-----------|-----------|----------|-------|-------------|
| `مدل` | MODEL | بله | - | مدلی که LoRA روی آن آموزش داده می‌شود. |
| `لاتنت‌ها` | LATENT | بله | - | لیتنت‌هایی که برای آموزش استفاده می‌شوند، به عنوان مجموعه داده/ورودی مدل عمل می‌کنند. |
| `شرط مثبت` | CONDITIONING | بله | - | شرطی‌سازی مثبت برای استفاده در آموزش. |
| `اندازه بچ` | INT | بله | 1-10000 | اندازه دسته برای استفاده در آموزش (پیش‌فرض: 1). |
| `تعداد مراحل انباشت گرادیان` | INT | بله | 1-1024 | تعداد مراحل انباشت گرادیان برای استفاده در آموزش (پیش‌فرض: 1). |
| `تعداد مراحل` | INT | بله | 1-100000 | تعداد مراحل برای آموزش LoRA (پیش‌فرض: 16). |
| `نرخ یادگیری` | FLOAT | بله | 0.0000001-1.0 | نرخ یادگیری برای استفاده در آموزش (پیش‌فرض: 0.0005). |
| `رتبه` | INT | بله | 1-128 | رتبه لایه‌های LoRA (پیش‌فرض: 8). |
| `بهینه‌ساز` | COMBO | بله | "AdamW"<br>"Adam"<br>"SGD"<br>"RMSprop" | بهینه‌سازی که برای آموزش استفاده می‌شود (پیش‌فرض: "AdamW"). |
| `تابع خطا` | COMBO | بله | "MSE"<br>"L1"<br>"Huber"<br>"SmoothL1" | تابع خطا برای استفاده در آموزش (پیش‌فرض: "MSE"). |
| `بذر` | INT | بله | 0-18446744073709551615 | دانه (seed) برای استفاده در آموزش (در مولد برای مقداردهی اولیه وزن LoRA و نمونه‌گیری نویز استفاده می‌شود) (پیش‌فرض: 0). |
| `نوع داده آموزش` | COMBO | بله | "bf16"<br>"fp32"<br>"none" | نوع داده (dtype) برای استفاده در آموزش. 'none' نوع محاسباتی اصلی مدل را حفظ می‌کند و آن را بازنویسی نمی‌کند. برای مدل‌های fp16، GradScaler به طور خودکار فعال می‌شود (پیش‌فرض: "bf16"). |
| `نوع داده LoRA` | COMBO | بله | "bf16"<br>"fp32" | نوع داده (dtype) برای استفاده در LoRA (پیش‌فرض: "bf16"). |
| `quantized_backward` | BOOLEAN | بله | - | هنگام استفاده از training_dtype 'none' و آموزش بر روی مدل کوانتایز شده، در صورت فعال بودن، backward با ضرب ماتریس کوانتایز شده انجام می‌شود (پیش‌فرض: False). |
| `الگوریتم` | COMBO | بله | گزینه‌های متعدد موجود | الگوریتمی که برای آموزش استفاده می‌شود. |
| `ذخیره‌سازی گرادیان` | BOOLEAN | بله | - | استفاده از ایست بازرسی گرادیان برای آموزش (پیش‌فرض: True). |
| `checkpoint_depth` | INT | بله | 1-5 | سطح عمق برای ایست بازرسی گرادیان (پیش‌فرض: 1). |
| `offloading` | BOOLEAN | بله | - | تخلیه وزن‌های مدل به CPU در طول آموزش برای صرفه‌جویی در حافظه GPU (پیش‌فرض: False). |
| `LoRA موجود` | COMBO | بله | گزینه‌های متعدد موجود | LoRA موجود برای الحاق به آن. برای LoRA جدید روی "[None]" تنظیم کنید (پیش‌فرض: "[None]"). |
| `حالت سطل‌بندی رزولوشن` | BOOLEAN | بله | - | فعال‌سازی حالت سطل وضوح (resolution bucket). در صورت فعال بودن، انتظار لیتنت‌های از پیش سطل‌بندی‌شده از گره ResolutionBucket را دارد (پیش‌فرض: False). |
| `bypass_mode` | BOOLEAN | بله | - | فعال‌سازی حالت عبور (bypass) برای آموزش. در صورت فعال بودن، آداپتورها از طریق هوک‌های پیشرو (forward hooks) به جای تغییر وزن اعمال می‌شوند. برای مدل‌های کوانتایز شده که وزن‌ها را نمی‌توان مستقیماً تغییر داد مفید است (پیش‌فرض: False). |

**توجه:** تعداد ورودی‌های شرطی‌سازی مثبت باید با تعداد تصاویر لیتنت مطابقت داشته باشد. اگر تنها یک شرطی‌سازی مثبت با چندین تصویر ارائه شود، به طور خودکار برای همه تصاویر تکرار می‌شود.

**توجه در مورد `training_dtype`:** هنگامی که روی "none" تنظیم می‌شود، نوع محاسباتی اصلی مدل حفظ می‌شود. برای مدل‌های fp16، GradScaler به طور خودکار فعال می‌شود تا از زیرریزی (underflow) در طول محاسبه گرادیان جلوگیری کند. اگر `fp16_accumulation` نیز فعال باشد (از طریق پرچم‌های `--fast`)، این ترکیب می‌تواند از نظر عددی ناپایدار باشد و باعث ایجاد مقادیر NaN شود.

**توجه در مورد `quantized_backward`:** این پارامتر تنها زمانی مرتبط است که `training_dtype` روی "none" تنظیم شده باشد و مدل یک مدل کوانتایز شده باشد. این پارامتر ضرب ماتریس کوانتایز شده را در طول پاس backward فعال می‌کند.

**توجه در مورد `bypass_mode`:** در صورت فعال بودن، آداپتورها از طریق هوک‌های پیشرو به جای تغییر مستقیم وزن‌های مدل اعمال می‌شوند. این امر به ویژه برای مدل‌های کوانتایز شده که وزن‌ها را نمی‌توان مستقیماً تغییر داد مفید است.

## خروجی‌ها

| نام خروجی | نوع داده | توضیحات |
|-------------|-----------|-------------|
| `lora` | LORA_MODEL | وزن‌های LoRA آموزش‌دیده که می‌توانند ذخیره شوند یا به مدل‌های دیگر اعمال شوند. |
| `loss_map` | LOSS_MAP | یک دیکشنری حاوی مقادیر خطای آموزشی در طول زمان. |
| `تعداد مراحل` | INT | تعداد کل مراحل آموزشی تکمیل‌شده (شامل هر مرحله قبلی از LoRA موجود). |

---
**Source fingerprint (SHA-256):** `df315ef416ff3ce81e6a526af2c4e5115980e6c35830825967e7189d4f8541d8`
