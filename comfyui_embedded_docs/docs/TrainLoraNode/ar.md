> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/TrainLoraNode/ar.md)

بالتأكيد، إليك الترجمة العربية للوثيقة التقنية الخاصة بعقدة TrainLoraNode في ComfyUI، مع الالتزام التام بقواعد الترجمة المحددة:

---

تقوم عقدة TrainLoraNode بإنشاء وتدريب نموذج LoRA (التكيف منخفض الرتبة) على نموذج انتشار باستخدام البيانات الكامنة (latents) وبيانات التكييف (conditioning) المقدمة. تتيح لك هذه العقدة ضبط نموذج بدقة باستخدام معاملات تدريب مخصصة، ومحسنات، ودوال خسارة. تُخرج العقدة أوزان LoRA المُدرّبة، وخريطة تاريخ الخسارة، وإجمالي خطوات التدريب المُنجزة.

## المدخلات

| المعامل | نوع البيانات | إلزامي | النطاق | الوصف |
|---|---|---|---|---|
| `نموذج` | MODEL | نعم | - | النموذج المراد تدريب LoRA عليه. |
| `مُختَزَلات` | LATENT | نعم | - | البيانات الكامنة (Latents) المستخدمة للتدريب، والتي تعمل كمجموعة بيانات/مدخل للنموذج. |
| `إيجابي` | CONDITIONING | نعم | - | التكييف الإيجابي المستخدم للتدريب. |
| `حجم الدُفعة` | INT | نعم | 1-10000 | حجم الدفعة (batch size) المستخدم للتدريب (القيمة الافتراضية: 1). |
| `خطوات تراكم التدرج` | INT | نعم | 1-1024 | عدد خطوات تراكم التدرج (gradient accumulation) المستخدمة للتدريب (القيمة الافتراضية: 1). |
| `خطوات` | INT | نعم | 1-100000 | عدد الخطوات لتدريب LoRA (القيمة الافتراضية: 16). |
| `معدل التعلم` | FLOAT | نعم | 0.0000001-1.0 | معدل التعلم المستخدم للتدريب (القيمة الافتراضية: 0.0005). |
| `الرتبة` | INT | نعم | 1-128 | رتبة (rank) طبقات LoRA (القيمة الافتراضية: 8). |
| `المحسن` | COMBO | نعم | "AdamW"<br>"Adam"<br>"SGD"<br>"RMSprop" | المحسّن المستخدم للتدريب (القيمة الافتراضية: "AdamW"). |
| `دالة الخسارة` | COMBO | نعم | "MSE"<br>"L1"<br>"Huber"<br>"SmoothL1" | دالة الخسارة المستخدمة للتدريب (القيمة الافتراضية: "MSE"). |
| `البذرة` | INT | نعم | 0-18446744073709551615 | البذرة (seed) المستخدمة للتدريب (تُستخدم في المولد لتهيئة أوزان LoRA وأخذ عينات الضوضاء) (القيمة الافتراضية: 0). |
| `نوع بيانات التدريب` | COMBO | نعم | "bf16"<br>"fp32"<br>"none" | نوع البيانات (dtype) المستخدم للتدريب. القيمة 'none' تحافظ على نوع الحساب الأصلي للنموذج بدلاً من تغييره. بالنسبة لنماذج fp16، يتم تمكين GradScaler تلقائيًا (القيمة الافتراضية: "bf16"). |
| `نوع بيانات LoRA` | COMBO | نعم | "bf16"<br>"fp32" | نوع البيانات (dtype) المستخدم لـ LoRA (القيمة الافتراضية: "bf16"). |
| `quantized_backward` | BOOLEAN | نعم | - | عند استخدام training_dtype بقيمة 'none' والتدريب على نموذج مكمّم (quantized)، يتم إجراء الانتشار العكسي (backward) باستخدام ضرب المصفوفات المكمّم (quantized matmul) عند التفعيل (القيمة الافتراضية: False). |
| `الخوارزمية` | COMBO | نعم | تتوفر خيارات متعددة | الخوارزمية المستخدمة للتدريب. |
| `التحقق من التدرج` | BOOLEAN | نعم | - | استخدام نقاط تفتيش التدرج (gradient checkpointing) للتدريب (القيمة الافتراضية: True). |
| `عمق نقطة الحفظ` | INT | نعم | 1-5 | مستوى العمق لنقاط تفتيش التدرج (القيمة الافتراضية: 1). |
| `تفريغ` | BOOLEAN | نعم | - | تفريغ أوزان النموذج إلى وحدة المعالجة المركزية (CPU) أثناء التدريب لتوفير ذاكرة وحدة معالجة الرسومات (GPU) (القيمة الافتراضية: False). |
| `LoRA الحالي` | COMBO | نعم | تتوفر خيارات متعددة | LoRA الحالي للإلحاق به. اضبط على "None" لإنشاء LoRA جديد (القيمة الافتراضية: "[None]"). |
| `وضع الدلو` | BOOLEAN | نعم | - | تفعيل وضع دلو الدقة (resolution bucket mode). عند التفعيل، يتوقع بيانات كامنة (latents) موضوعة مسبقًا في دلاء من عقدة ResolutionBucket (القيمة الافتراضية: False). |
| `bypass_mode` | BOOLEAN | نعم | - | تفعيل وضع التجاوز (bypass mode) للتدريب. عند التفعيل، يتم تطبيق المحولات (adapters) عبر خطافات أمامية (forward hooks) بدلاً من تعديل الأوزان. مفيد للنماذج المكمّمة (quantized models) حيث لا يمكن تعديل الأوزان مباشرة (القيمة الافتراضية: False). |

**ملاحظة:** يجب أن يتطابق عدد مدخلات التكييف الإيجابي مع عدد الصور الكامنة (latent images). إذا تم توفير تكييف إيجابي واحد فقط مع صور متعددة، فسيتم تكراره تلقائيًا لجميع الصور.

**ملاحظة حول `training_dtype`:** عند الضبط على "none"، يتم الحفاظ على نوع الحساب الأصلي للنموذج. بالنسبة لنماذج fp16، يتم تمكين GradScaler تلقائيًا لمنع التدفق السفلي (underflow) أثناء حساب التدرج. إذا تم أيضًا تمكين `fp16_accumulation` (عبر علامات `--fast`)، فقد يكون هذا المزيج غير مستقر عدديًا وقد يتسبب في قيم NaN.

**ملاحظة حول `quantized_backward`:** هذا المعامل ذو صلة فقط عندما يتم ضبط `training_dtype` على "none" وكان النموذج نموذجًا مكمّمًا (quantized model). فهو يمكّن ضرب المصفوفات المكمّم (quantized matrix multiplication) أثناء مرور الانتشار العكسي.

**ملاحظة حول `bypass_mode`:** عند التفعيل، يتم تطبيق المحولات (adapters) عبر خطافات أمامية (forward hooks) بدلاً من تعديل أوزان النموذج مباشرة. هذا مفيد بشكل خاص للنماذج المكمّمة (quantized models) حيث لا يمكن تعديل الأوزان مباشرة.

## المخرجات

| اسم المخرج | نوع البيانات | الوصف |
|---|---|---|
| `خريطة الخسارة` | LORA_MODEL | أوزان LoRA المُدرّبة التي يمكن حفظها أو تطبيقها على نماذج أخرى. |
| `الخطوات` | LOSS_MAP | قاموس يحتوي على قيم خسارة التدريب بمرور الوقت. |
| `خطوات` | INT | إجمالي عدد خطوات التدريب المُنجزة (بما في ذلك أي خطوات سابقة من LoRA موجود). |

---
**Source fingerprint (SHA-256):** `df315ef416ff3ce81e6a526af2c4e5115980e6c35830825967e7189d4f8541d8`
