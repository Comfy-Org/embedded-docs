# BriaGenFill

این گره با استفاده از API بریا، اشیاء یا صحنه‌هایی را در ناحیه ماسک‌شده یک تصویر تولید می‌کند. تصویر و ماسک را آپلود کرده، پرامپت را به سرویس پر کردن مولد بریا ارسال می‌کند، منتظر تکمیل عملیات می‌ماند و تصویر ویرایش‌شده را بازمی‌گرداند. این یک عملیات پولی API است (۰٫۰۴۲۹ دلار آمریکا به ازای هر درخواست).
## ورودیها

### ورودیهای مشترک

| پارامتر | توضیحات | نوع داده | اجباری | محدوده |
|---|---|---|---|---|
| `image` | تصویر ورودی برای ویرایش. | IMAGE | بله | - |
| `mask` | نواحی سفید با محتوای تولیدشده پر می‌شوند و نواحی سیاه حفظ می‌شوند. ماسک قبل از ارسال دودویی می‌شود، بنابراین نواحی که به صورت جزئی رنگ‌آمیزی شده‌اند سفید محسوب می‌شوند. باید نسبت تصویر یکسانی با تصویر داشته باشد. | MASK | بله | - |
| `prompt` | توضیحاتی درباره آنچه باید در ناحیه ماسک‌شده تولید شود. باید حداقل ۱ کاراکتر باشد. | STRING | بله | - |
| `negative_prompt` | پرامپتی که محتوای نامطلوب در نتیجه تولید را توصیف می‌کند. اگر خالی بماند، به API ارسال نمی‌شود. | STRING | بله | - |
| `refine_prompt` | پرامپت را به‌صورت خودکار برای نتایج بهتر تنظیم می‌کند؛ برای استفاده از پرامپت دقیقاً همان‌طور که نوشته شده، آن را غیرفعال کنید. (پیش‌فرض: true) | BOOLEAN | بله | true<br>false |
| `seed` | دانه (Seed) برای فرایند تولید. (پیش‌فرض: 42) | INT | بله | 1 to 2147483647 |
| `moderation` | تنظیمات پالایش محتوا برای درخواست. وقتی روی "true" تنظیم شود، گزینه‌های تو در توی پالایش که در زیر توضیح داده شده‌اند اعمال می‌شوند. (پیش‌فرض: "false") | DYNAMIC_COMBO | بله | "false"<br>"true" |

### ورودیهای نظارت

| پارامتر | توضیحات | نوع داده | اجباری | محدوده |
|---|---|---|---|---|
| `prompt_content_moderation` | پالایش محتوا را روی پرامپت اعمال می‌کند. (پیش‌فرض: false) | BOOLEAN | خیر | true<br>false |
| `visual_input_moderation` | پالایش محتوا را روی تصویر ورودی اعمال می‌کند. (پیش‌فرض: false) | BOOLEAN | خیر | true<br>false |
| `visual_output_moderation` | پالایش محتوا را روی تصویر خروجی اعمال می‌کند. (پیش‌فرض: false) | BOOLEAN | خیر | true<br>false |

## خروجیها

| نام خروجی | توضیحات | نوع داده |
|---|---|---|
| `image` | The resulting image with the masked region filled by the generated content. | IMAGE |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/BriaGenFill/fa.md)

---
**Source fingerprint (SHA-256):** `0d9babfa5e14c03f73d2b5befbd1c5cd1f5ffc685a0d7ccb3db09cfec51ba4fa`
