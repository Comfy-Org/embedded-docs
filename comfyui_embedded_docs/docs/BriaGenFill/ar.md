# BriaGenFill

هذه العقدة تولّد كائنات أو مشاهد داخل منطقة محدّدة بقناع (Mask) في صورة ما عبر استخدام واجهة برمجة تطبيقات Bria. تقوم برفع الصورة والقناع، وإرسال النص الموجّه إلى خدمة التعبئة التوليدية من Bria، ثم تنتظر اكتمال العملية، وتُعيد الصورة المعدّلة. تُعتبر هذه عملية مدفوعة التكلفة (0.0429 دولار أمريكي لكل طلب).
## المدخلات

### المدخلات الشائعة

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
|---|---|---|---|---|
| `image` | الصورة المدخلة المراد تعديلها. | IMAGE | نعم | - |
| `mask` | المناطق البيضاء يتم ملؤها بالمحتوى المُولَّد، بينما تُحفظ المناطق السوداء كما هي. يتم تحويل القناع إلى صيغة ثنائية قبل الإرسال، لذا فإن المناطق المطلية جزئيًا تُعتبر بيضاء. يجب أن تكون نسبة الأبعاد مطابقة لنسبة أبعاد الصورة. | MASK | نعم | - |
| `prompt` | وصف ما يجب توليده داخل المنطقة المحددة بالقناع. يجب أن يحتوي على حرف واحد على الأقل. | STRING | نعم | - |
| `negative_prompt` | نص موجّه يصف المحتوى الذي يجب تجنّبه في النتيجة المُولَّدة. إذا تُرك فارغًا، فلن يتم إرساله إلى الواجهة البرمجية. | STRING | نعم | - |
| `refine_prompt` | ضبط النص الموجّه تلقائيًا للحصول على نتائج أفضل؛ قم بتعطيل هذا الخيار لاستخدام النص الموجّه كما هو مكتوب تمامًا. (الافتراضي: true) | BOOLEAN | نعم | true<br>false |
| `seed` | البذرة المستخدمة في عملية التوليد. (الافتراضي: 42) | INT | نعم | 1 to 2147483647 |
| `الإشراف` | إعدادات المراقبة للطلب. عند ضبطها على "true"، يتم تطبيق خيارات المراقبة المتداخلة الموضحة أدناه. (الافتراضي: "false") | DYNAMIC_COMBO | نعم | "false"<br>"true" |

### مدخلات الإشراف

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
|---|---|---|---|---|
| `prompt_content_moderation` | يطبّق مراقبة المحتوى على النص الموجّه. (الافتراضي: false) | BOOLEAN | لا | true<br>false |
| `visual_input_moderation` | يطبّق مراقبة المحتوى على الصورة المدخلة. (الافتراضي: false) | BOOLEAN | لا | true<br>false |
| `visual_output_moderation` | يطبّق مراقبة المحتوى على الصورة المُخرجة. (الافتراضي: false) | BOOLEAN | لا | true<br>false |

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
|---|---|---|
| `image` | The resulting image with the masked region filled by the generated content. | IMAGE |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/BriaGenFill/ar.md)

---
**Source fingerprint (SHA-256):** `0d9babfa5e14c03f73d2b5befbd1c5cd1f5ffc685a0d7ccb3db09cfec51ba4fa`
