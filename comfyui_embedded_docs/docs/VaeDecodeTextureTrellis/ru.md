# VaeDecodeTextureTrellis

Этот узел декодирует текстурный латент Trellis2 в цвета вокселей с помощью VAE. Входной латент содержит разреженные образцы признаков с координатами; узел восстанавливает цвет для каждого вокселя и возвращает результат в виде воксельной сетки, которую последующие узлы, такие как PaintMesh, могут использовать для окраски 3D-сетки.

## Входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
|-----------|-------------|-----------|----------|-------|
| `samples` | Текстурный латент для декодирования. Содержит образцы признаков и разреженные координаты, а также может включать дополнительные метаданные, такие как количество координат, кадр модели и разрешение координат. | LATENT | Да | — |
| `vae` | VAE Trellis2, используемый для декодирования текстурного латента в цвета вокселей. | VAE | Да | — |
| `shape_subdivides` | Информация о форме, используемая для управления реконструкцией с более высокой детализацией при декодировании. Помогает сохранять согласованность структуры при высоких разрешениях. | SHAPE_SUBDIVIDES | Да | — |

Примечание: если латент `samples` содержит количество координат, то эти значения должны быть неотрицательными, их сумма должна совпадать с числом строк координат, и каждый батч должен содержать ровно ожидаемое число строк; в противном случае узел вызывает ошибку. Если системой координат модели в латенте является "z_up", декодированные координаты вокселей преобразуются в Y-up, чтобы они совпадали с вершинами сетки. Когда указано разрешение координат, разрешение выходной текстуры равно этому значению, умноженному на 16; в противном случае оно вычисляется из наибольшей координаты вокселя и округляется вверх до одного из значений 256, 512, 1024, 1536 или 2048 (1024, если координаты недоступны).

## Выходы

| Имя выхода | Описание | Тип данных |
|-------------|-------------|-----------|
| `voxel_colors` | Декодированные данные вокселей, содержащие координаты, цветовые признаки и разрешение текстуры. Каждый воксель имеет 6 цветовых каналов: базовый цвет (RGB), металличность, шероховатость и альфа, все в диапазоне [0, 1]. Потребители вершинных цветов, такие как PaintMesh, используют первые 3 канала. | VOXEL |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/VaeDecodeTextureTrellis/ru.md)

---
**Source fingerprint (SHA-256):** `cfbe59efb18d2c3c7c597c5212900fea54d660aa98005817debf4711401a6967`
