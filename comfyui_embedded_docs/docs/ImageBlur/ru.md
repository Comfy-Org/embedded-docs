> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ImageBlur/ru.md)

Узел `ImageBlur` применяет размытие по Гауссу к изображению, позволяя смягчить края, уменьшить детализацию и шум. Он обеспечивает контроль над интенсивностью и распространением размытия через параметры.

## Входные параметры

| Поле            | Тип данных | Описание                                                                   |
|----------------|-------------|-------------------------------------------------------------------------------|
| `image`        | `IMAGE`     | Входное изображение для размытия. Это основной объект применения эффекта размытия. |
| `blur_radius`  | `INT`       | Определяет радиус эффекта размытия. Больший радиус приводит к более выраженному размытию. |
| `sigma`        | `FLOAT`     | Управляет распространением размытия. Более высокое значение sigma означает, что размытие будет затрагивать более широкую область вокруг каждого пикселя. |

## Выходные параметры

| Поле | Тип данных | Описание                                                              |
|-------|-------------|--------------------------------------------------------------------------|
| `image`| `IMAGE`     | На выходе получается размытая версия входного изображения, степень размытия определяется входными параметрами. |