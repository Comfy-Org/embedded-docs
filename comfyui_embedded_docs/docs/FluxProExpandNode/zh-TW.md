> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/FluxProExpandNode/zh-TW.md)

根據提示詞對圖像進行外繪擴展。此節點透過在圖像的上、下、左、右側添加像素來擴展圖像，同時生成與提供的文字描述相符的新內容。

## 輸入

| 參數 | 資料類型 | 必要 | 範圍 | 說明 |
|-----------|-----------|----------|-------|-------------|
| `影像` | IMAGE | 是 | - | 要擴展的輸入圖像 |
| `提示詞` | STRING | 否 | - | 圖像生成的提示詞（預設值：""） |
| `提示詞向上採樣` | BOOLEAN | 否 | - | 是否對提示詞進行上取樣。若啟用，會自動修改提示詞以產生更具創意的生成結果，但結果不具有確定性（相同的種子不會產生完全相同的結果）。（預設值：False） |
| `上方` | INT | 否 | 0-2048 | 在圖像頂部擴展的像素數量（預設值：0） |
| `下方` | INT | 否 | 0-2048 | 在圖像底部擴展的像素數量（預設值：0） |
| `左側` | INT | 否 | 0-2048 | 在圖像左側擴展的像素數量（預設值：0） |
| `右側` | INT | 否 | 0-2048 | 在圖像右側擴展的像素數量（預設值：0） |
| `引導強度` | FLOAT | 否 | 1.5-100 | 圖像生成過程的引導強度（預設值：60） |
| `步驟數` | INT | 否 | 15-50 | 圖像生成過程的步數（預設值：50） |
| `種子` | INT | 否 | 0-18446744073709551615 | 用於建立雜訊的隨機種子。（預設值：0） |

## 輸出

| 輸出名稱 | 資料類型 | 說明 |
|-------------|-----------|-------------|
| `影像` | IMAGE | 擴展後的輸出圖像 |

---
**Source fingerprint (SHA-256):** `15b21f1de8a98a6bcde131a61c01b062434c6a959bc563550d613972412973fe`
