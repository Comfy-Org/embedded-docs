# Comfy Cloud Z-Image Turbo 文字轉圖片 [BETA]

此節點使用 Z-Image Turbo 模型，根據文字提示生成影像，且僅需 8 個步驟即可完成。生成作業會在 Comfy Cloud GPU 上遠端執行，並依執行時間計費，這使其成為此處用於反覆嘗試影像構想最快速且最便宜的選項之一。生成完成後，節點會下載完成的影像，以供您的工作流程使用。此節點屬於 Comfy Cloud 節點集，該節點集標記為 BETA：選項可能會新增或移除，且工作流程可能會被停用。

## 輸入

| 參數 | 描述 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `提示詞` | 用於描述要生成影像的文字提示。接受多行輸入，並在提交前去除前後空白。去除空白後不得為空。預設值：""（空）。 | STRING | 是 | 1 - 4096 characters |
| `種子` | 用於控制生成可重現性的隨機種子。變更它會產生不同的變化。包含生成後控制選項。預設值：42。 | INT | 否 | 0 - 18446744073709551615 |
| `長寬比` | 生成影像的長寬比。預設值："1:1"。 | COMBO | 否 | "1:1"<br>"3:4"<br>"2:3"<br>"3:2"<br>"4:3"<br>"16:9"<br>"9:16"<br>"21:9" |
| `百萬像素` | 總像素預算。在正方形比例下，1.0 約為 1024x1024。預設值：1.0。 | FLOAT | 否 | 0.1 - 16.0<br>(step of 0.1) |

注意：輸入值會在提交生成前進行驗證。`prompt` 在去除空白後必須包含 1 到 4,096 個字元，`aspect_ratio` 必須是所列選項之一，且 `megapixels` 必須以 0.1 為增量輸入。

## 輸出

| 輸出名稱 | 描述 | 資料類型 |
|-------------|-------------|-----------|
| `IMAGE` | 生成的影像以影像張量形式傳回，可供進一步的影像處理或儲存節點使用。 | IMAGE |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ComfyCloudZImageTurboNode/zh-TW.md)

---
**Source fingerprint (SHA-256):** `9c78bf9aca5800212d1c5a8f9581dc6c154a82220cd60a8b55ebe74111d2f542`
