> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [Edit on GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ImageUpscaleWithModel/zh-TW.md)

此節點專為使用指定的放大模型來提升影像解析度而設計。它能有效率地管理放大流程，透過將影像調整至適當的裝置、優化記憶體使用量，並以分塊方式套用放大模型，以避免潛在的記憶體不足錯誤。

## 輸入參數

| 參數名稱         | Comfy 資料類型    | 描述                                                                 |
|-------------------|-------------------|----------------------------------------------------------------------------|
| `放大模型`   | `UPSCALE_MODEL`   | 用於影像放大的放大模型。此參數對於定義放大演算法及其參數至關重要。 |
| `影像`           | `IMAGE`           | 需要進行放大的原始影像。此輸入決定了將進行放大處理的來源內容。 |

## 輸出結果

| 參數名稱 | 資料類型 | 描述                                        |
|-----------|-------------|----------------------------------------------------|
| `影像`   | `IMAGE`     | 經放大模型處理後的高解析度影像。此輸出是放大操作的最終結果，展示了提升後的解析度或畫質。 |