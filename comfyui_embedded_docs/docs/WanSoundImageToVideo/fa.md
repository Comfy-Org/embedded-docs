> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/WanSoundImageToVideo/fa.md)

## ورودی‌ها

| پارامتر | نوع داده | الزامی | محدوده | توضیحات |
|---------|----------|--------|--------|----------|
| `مثبت` | CONDITIONING | بله | - | راهنمای شرطی‌سازی مثبت که مشخص می‌کند چه محتوایی باید در ویدیوی تولیدشده ظاهر شود |
| `منفی` | CONDITIONING | بله | - | راهنمای شرطی‌سازی منفی که مشخص می‌کند چه محتوایی باید در ویدیوی تولیدشده اجتناب شود |
| `vae` | VAE | بله | - | مدل VAE مورد استفاده برای رمزگذاری و رمزگشایی بازنمایی‌های نهان ویدیو |
| `عرض` | INT | بله | 16 تا MAX_RESOLUTION | عرض ویدیوی خروجی بر حسب پیکسل (پیش‌فرض: 832، باید بر 16 بخش‌پذیر باشد) |
| `ارتفاع` | INT | بله | 16 تا MAX_RESOLUTION | ارتفاع ویدیوی خروجی بر حسب پیکسل (پیش‌فرض: 480، باید بر 16 بخش‌پذیر باشد) |
| `طول` | INT | بله | 1 تا MAX_RESOLUTION | تعداد فریم‌های ویدیوی تولیدشده (پیش‌فرض: 77، باید بر 4 بخش‌پذیر باشد) |
| `اندازه بچ` | INT | بله | 1 تا 4096 | تعداد ویدیوهایی که به‌طور همزمان تولید می‌شوند (پیش‌فرض: 1) |
| `خروجی انکودر صوتی` | AUDIOENCODEROUTPUT | خیر | - | رمزگذاری صوتی اختیاری که می‌تواند بر اساس ویژگی‌های صوتی بر تولید ویدیو تأثیر بگذارد |
| `تصویر مرجع` | IMAGE | خیر | - | تصویر مرجع اختیاری که راهنمای بصری برای محتوای ویدیو ارائه می‌دهد |
| `کنترل ویدیو` | IMAGE | خیر | - | ویدیوی کنترلی اختیاری که حرکت و ساختار ویدیوی تولیدشده را هدایت می‌کند |
| `حرکت مرجع` | IMAGE | خیر | - | مرجع حرکت اختیاری که راهنمایی برای الگوهای حرکتی در ویدیو ارائه می‌دهد |

## خروجی‌ها

| نام خروجی | نوع داده | توضیحات |
|-----------|----------|----------|
| `مثبت` | CONDITIONING | شرطی‌سازی مثبت پردازش‌شده که برای تولید ویدیو اصلاح شده است |
| `منفی` | CONDITIONING | شرطی‌سازی منفی پردازش‌شده که برای تولید ویدیو اصلاح شده است |
| `latent` | LATENT | بازنمایی ویدیوی تولیدشده در فضای نهان که می‌تواند به فریم‌های نهایی ویدیو رمزگشایی شود |

---
**Source fingerprint (SHA-256):** `f80f82b8671294a14ecfecf91bc13febae0c91c5efa438467a4413d52dc82d3f`
