# GetItemFromList

此節點從列表中返回單個項目，該項目由其位置選擇。您提供列表以及所需項目的索引編號，節點便會輸出該項目。這使得它適用於從一組值中挑選特定元素，例如從一批影像中挑選特定的一張。

## 輸入

| 參數 | 描述 | 資料類型 | 必需 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `list` | 要從中選擇的值列表。由於此節點被標記為接受列表輸入，連接的值會被視為一組項目。 | Any Type | 是 | 任意值列表 |
| `index` | 要返回的項目的位置。值從 0 開始，因此 `0` 返回第一個項目，`1` 返回第二個項目，依此類推（預設值：0）。 | INT | 是 | 任意整數索引 |

## 輸出

| 輸出名稱 | 描述 | 資料類型 |
|-------------|-------------|-----------|
| `OUTPUT` | 位於所提供 `list` 中給定 `index` 處的單個項目。 | Any Type |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/GetItemFromList/zh-TW.md)

---
**Source fingerprint (SHA-256):** `11c1c90fed0e29f1110b4c1dda64d60797aff38d7f3af69f76b74e94fe94e976`
