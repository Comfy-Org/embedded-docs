# Tripo: Ретопология

Tripo: Retopology принимает высокополигональную 3D-модель, созданную предыдущим узлом Tripo, и перестраивает её в низкополигональную версию с чистой топологией. Он отправляет модель в сервис ретопологии Tripo, ожидает завершения задачи, затем скачивает готовую модель и предоставляет её ID задачи для использования другими узлами Tripo.

## Входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
|-----------|-------------|-----------|----------|-------|
| `model_task_id` | ID задачи исходной высокополигональной модели. Принимает ID задачи модели от узла генерации Tripo или ID задачи сегмента от Tripo: Segment Model. | STRING | Да | Tripo task ID |
| `face_limit` | Целевое количество граней: 500–20 000 треугольников или 500–10 000 четырёхугольников. -1 позволяет Tripo выбрать самостоятельно. (по умолчанию: -1) | INT | Да | -1 (автоматически)<br>500–20 000 (треугольники)<br>500–10 000 (четырёхугольники) |
| `quad` | Вывод четырёхугольной сетки. Tripo выдаёт четырёхугольные сетки в формате FBX, поэтому результат появляется на выходе FBX, а выход GLB остаётся пустым. (по умолчанию: False) | BOOLEAN | Да | True<br>False (по умолчанию) |
| `bake` | Запекает исходные текстуры на низкополигональную сетку. (по умолчанию: True) | BOOLEAN | Нет | True (по умолчанию)<br>False |
| `part_names` | Имена частей через запятую из Tripo: Segment Model. Пустое значение обрабатывает всю модель. (по умолчанию: "") | STRING | Нет | Имена частей модели или пусто |

Примечание: Когда `face_limit` имеет значение -1, Tripo автоматически определяет количество граней. Когда `quad` включён, максимальное ограничение количества граней составляет 10 000 четырёхугольников вместо 20 000 треугольников, а результат предоставляется в формате FBX (выход GLB остаётся пустым). Когда `part_names` пуст, обрабатывается вся модель. Если `face_limit` имеет значение, отличное от -1, и выходит за допустимый диапазон, узел выдаёт ошибку.

## Выходы

| Имя выхода | Описание | Тип данных |
|-------------|-------------|-----------|
| `model_file` | Обратно совместимый выход, который идентифицирует завершённый файл модели. В более новых рабочих процессах следует использовать выходы GLB или FBX. | STRING |
| `model task_id` | ID задачи готового результата ретопологии. Может быть передан другим узлам Tripo для ссылки на эту модель. | STRING |
| `GLB` | Ретопологизированная низкополигональная модель в формате GLB. Пустой, когда `quad` включён. | GLB FILE |
| `FBX` | Ретопологизированная низкополигональная модель в формате FBX. Заполняется только когда `quad` включён. | FBX FILE |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/TripoRetopologyNode/ru.md)

---
**Source fingerprint (SHA-256):** `b0e967eb4987a70242b6cfce93f09e0caffb7f4bdd3e4f1439e68f33f9138bb5`
