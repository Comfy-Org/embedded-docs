# Luma UNI-1 Image

Этот узел генерирует изображения по текстовым описаниям с использованием модели Luma UNI-1. Он принимает текстовый промпт и необязательные настройки, такие как соотношение сторон и стиль, затем отправляет запрос к Luma API для создания изображения. Доступны два варианта модели: `uni-1` и `uni-1-max`.

## Входы

### Общие входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `model` | Модель, используемая для генерации. Выбор модели открывает дополнительные настройки для этой модели. | DYNAMIC_COMBO | Да | `"uni-1"`<br>`"uni-1-max"` |
| `prompt` | Текстовое описание желаемого изображения. От 1 до 6000 символов. (по умолчанию: "") | STRING | Да | от 1 до 6000 символов |
| `seed` | Seed управляет тем, должен ли узел запускаться заново; результаты недетерминированы независимо от seed. (по умолчанию: 0) | INT | Да | от 0 до 2147483647 |

### Входы uni-1 и uni-1-max

Общие для вариантов моделей `uni-1` и `uni-1-max`. Эти настройки появляются при выборе любой из этих моделей.

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `aspect_ratio` | Соотношение сторон выходного изображения. `"auto"` позволяет модели выбирать его на основе промпта. (по умолчанию: `"auto"`) | COMBO | Да | `"auto"`<br>`"3:1"`<br>`"2:1"`<br>`"16:9"`<br>`"3:2"`<br>`"1:1"`<br>`"2:3"`<br>`"9:16"`<br>`"1:2"`<br>`"1:3"` |
| `style` | Пресет стиля. `"auto"` выбирает стиль на основе промпта; `"manga"` применяет эстетику манги/аниме и требует портретное соотношение сторон (2:3, 9:16, 1:2, 1:3). (по умолчанию: `"auto"`) | COMBO | Да | `"auto"`<br>`"manga"` |
| `web_search` | Искать в интернете визуальные референсы перед генерацией. (по умолчанию: False) | BOOLEAN | Да | True / False |

### Эталонные входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `image_ref` | Расширяемый слот: подключите от 1 до 9 элементов (например, `image_1` – `image_9`). До 9 референсных изображений для задания стиля/содержимого. | IMAGE | Нет | До 9 изображений |

**Примечание:** Если для `style` задано значение `"manga"`, параметр `aspect_ratio` должен быть либо `"auto"`, либо одним из портретных соотношений `"2:3"`, `"9:16"`, `"1:2"`, `"1:3"`. Использование любого другого соотношения со стилем `"manga"` вызовет ошибку. Максимальное количество референсных изображений — 9 для обеих моделей `uni-1` и `uni-1-max`.

## Выходы

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `image` | Сгенерированное изображение в виде тензора. | IMAGE |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LumaImageNode2/ru.md)

---
**Source fingerprint (SHA-256):** `27254fe4627fd340426a68f651cab4513ffb6668cafc0accd17f2c442f7d3125`
