# LTXVSpatioTemporalGuidance

Этот узел улучшает пространственную детализацию и согласованность движения при генерации видео LTXV, выполняя дополнительный проход на каждом шаге сэмплирования. Во время этого прохода самовнимание выбранных блоков трансформера деградирует до сквозной передачи значений, а генерация направляется в сторону от деградировавшего результата.

## Входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
|-----------|-------------|-----------|----------|-------|
| `model` | Базовая модель, к которой применяется пространственно-временное управление. Модель клонируется и модифицируется с помощью функции управления, применяемой после CFG. | MODEL | Да | — |
| `scale` | Сила управления, применяемого к результату денойзинга. При значении 0 управление не действует. (по умолчанию: 1.0) | FLOAT | Да | 0.0 до 100.0 (шаг 0.01) |
| `blocks` | Разделённые запятыми индексы блоков трансформера для изменения. Используются только числовые значения; все остальные символы игнорируются. (по умолчанию: "29") | STRING | Да | — |
| `start_percent` | Доля процесса сэмплирования, с которой начинается управление. (по умолчанию: 0.0) | FLOAT | Да | 0.0 до 1.0 (шаг 0.001) |
| `end_percent` | Доля процесса сэмплирования, на которой управление заканчивается. (по умолчанию: 1.0) | FLOAT | Да | 0.0 до 1.0 (шаг 0.001) |

Примечание: Управление применяется только в интервале сэмплирования между `start_percent` и `end_percent`. Если `scale` равно 0 или `blocks` не содержит числовых значений, управляемый проход не влияет на процесс сэмплирования.

## Выходы

| Имя выхода | Описание | Тип данных |
|-------------|-------------|-----------|
| `MODEL` | Клонированная модель с присоединённой функцией пространственно-временного управления. | MODEL |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LTXVSpatioTemporalGuidance/ru.md)

---
**Source fingerprint (SHA-256):** `0e14137b3bf416d36005b6b4b6db46495b1523f88b2bf574e2dc582175422a48`
