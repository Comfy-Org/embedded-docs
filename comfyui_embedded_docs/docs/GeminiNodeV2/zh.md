# Google Gemini

使用 Google Gemini 模型生成文本响应。提供文本提示，并可选择将一个或多个图像、音频片段、视频或文件作为多模态上下文。

## 输入

### 通用输入

| 参数 | 说明 | 数据类型 | 必填 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `model` | 用于生成响应的 Gemini 模型。 | COMBO | 是 | `"Gemini 3.5 Flash"`<br>`"Gemini 3.1 Pro"`<br>`"Gemini 3.1 Flash-Lite"` |
| `prompt` | 输入给模型的文本。可包含详细指令、问题或上下文。必须至少包含一个非空白字符。（默认：""） | STRING | 是 |  |
| `seed` | 采样种子。设置为 0 表示使用随机种子。不保证确定性输出。（默认：42） | INT | 是 | 0 到 2147483647 |
| `system_prompt` | 决定模型行为的基础指令。（默认：""） | STRING | 否 |  |

### Gemini 3.5 Flash 输入

当 `model` 设置为 `"Gemini 3.5 Flash"` 时，将显示这些输入。

| 参数 | 说明 | 数据类型 | 必填 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前进行内部推理的强度。HIGH 可提升困难任务的质量，但会消耗更多（思考）令牌且速度更慢。（默认："MEDIUM"） | COMBO | 是 | `"MINIMAL"`<br>`"LOW"`<br>`"MEDIUM"`<br>`"HIGH"` |
| `temperature` | 控制随机性。较低的值更专注/确定性更强，较高的值更具创造性。（默认：1.0） | FLOAT | 是 | 0.0 到 2.0 |
| `top_p` | 核采样：从累积概率达到 top_p 的最小令牌集合中采样。（默认：0.95） | FLOAT | 是 | 0.0 到 1.0 |
| `max_output_tokens` | 生成的最大令牌数，包括模型的内部思考。使用 thinking_level HIGH 时，较低的值可能没有剩余空间用于回答；如果响应为空或被截断，请提高此值。模型在完成时会提前停止，因此对于较短的回复，设得更高不会产生额外成本。（默认：32768） | INT | 是 | 16 到 65536 |

### Gemini 3.1 Pro 输入

当 `model` 设置为 `"Gemini 3.1 Pro"` 时，将显示这些输入。

| 参数 | 说明 | 数据类型 | 必填 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前进行内部推理的强度。HIGH 可提升困难任务的质量，但会消耗更多（思考）令牌且速度更慢。（默认："HIGH"） | COMBO | 是 | `"LOW"`<br>`"HIGH"` |
| `temperature` | 控制随机性。较低的值更专注/确定性更强，较高的值更具创造性。（默认：1.0） | FLOAT | 是 | 0.0 到 2.0 |
| `top_p` | 核采样：从累积概率达到 top_p 的最小令牌集合中采样。（默认：0.95） | FLOAT | 是 | 0.0 到 1.0 |
| `max_output_tokens` | 生成的最大令牌数，包括模型的内部思考。使用 thinking_level HIGH 时，较低的值可能没有剩余空间用于回答；如果响应为空或被截断，请提高此值。模型在完成时会提前停止，因此对于较短的回复，设得更高不会产生额外成本。（默认：32768） | INT | 是 | 16 到 65536 |

### Gemini 3.1 Flash-Lite 输入

当 `model` 设置为 `"Gemini 3.1 Flash-Lite"` 时，将显示这些输入。

| 参数 | 说明 | 数据类型 | 必填 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前进行内部推理的强度。HIGH 可提升困难任务的质量，但会消耗更多（思考）令牌且速度更慢。（默认："LOW"） | COMBO | 是 | `"LOW"`<br>`"HIGH"` |
| `temperature` | 控制随机性。较低的值更专注/确定性更强，较高的值更具创造性。（默认：1.0） | FLOAT | 是 | 0.0 到 2.0 |
| `top_p` | 核采样：从累积概率达到 top_p 的最小令牌集合中采样。（默认：0.95） | FLOAT | 是 | 0.0 到 1.0 |
| `max_output_tokens` | 生成的最大令牌数，包括模型的内部思考。使用 thinking_level HIGH 时，较低的值可能没有剩余空间用于回答；如果响应为空或被截断，请提高此值。模型在完成时会提前停止，因此对于较短的回复，设得更高不会产生额外成本。（默认：32768） | INT | 是 | 16 到 65536 |

### 媒体和文件输入

以下输入由所有三个模型共享，并与特定于模型的输入一同显示。

| 参数 | 说明 | 数据类型 | 必填 | 范围 |
|-----------|-------------|-----------|----------|-------|
| `images` | 可增长插槽：连接 1 到 16 张图像（`image_1` ... `image_16`）。可选的一幅或多幅图像，用作模型的上下文。 | IMAGE | 否 | 0 到 16 张图像 |
| `audio` | 可增长插槽：连接一个音频片段（`audio_1`）。可选的音频片段，用作模型的上下文。 | AUDIO | 否 | 0 到 1 个片段 |
| `video` | 可增长插槽：连接一个视频片段（`video_1`）。可选的视频片段，用作模型的上下文。 | VIDEO | 否 | 0 到 1 个片段 |
| `files` | 可选的文件，用作模型的上下文。接受来自 Gemini Input Files 节点的输入。 | GEMINI_INPUT_FILES | 否 |  |

**注意：** 当附加媒体（图像、音频或视频）时，节点会将前 10 个媒体项目上传到 ComfyAPI 存储，并以 URL 形式传递；此 URL 预算由所有媒体类型共享，并按顺序消耗（先视频，再音频，最后图像）。其余媒体将以内联方式编码为 base64 数据，内联合并负载上限为 18 MB。如果内联负载超过 18 MB，节点将引发错误。`prompt` 参数必须至少包含一个非空白字符。将 `seed` 设置为 0 可请求随机种子。

## 输出

| 输出名 | 说明 | 数据类型 |
|-------------|-------------|-----------|
| `output` | 来自 Gemini 模型的生成文本响应。如果模型未生成文本，则返回字符串 “Empty response from Gemini model...”。 | STRING |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/GeminiNodeV2/zh.md)

---
**Source fingerprint (SHA-256):** `e88c253d9ae987ab91b0fb6b0b55cfd9cd3671438770afcedd844f236b30dc36`
