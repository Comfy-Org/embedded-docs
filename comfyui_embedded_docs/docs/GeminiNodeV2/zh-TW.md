# Google Gemini

使用 Google 的 Gemini 模型生成文字回應。提供文字提示，並可選提供一或多個影像、音訊片段、影片或檔案作為多模態上下文。此節點會將提示及任何附加媒體傳送給所選模型，並傳回模型的文字回答。

**注意：** 此節點在原始碼中標記為已棄用。

## 輸入

### 通用輸入

| 參數 | 描述 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `model` | 用於生成回應的 Gemini 模型。選取模型後，會在下方顯示其專屬的一組輸入。 | DYNAMIC_COMBO | 是 | `"Gemini 3.8 Flash"`<br>`"Gemini 3.7 Flash"`<br>`"Gemini 3.5 Flash"`<br>`"Gemini 3.1 Pro"`<br>`"Gemini 3.1 Flash-Lite"` |
| `prompt` | 提供給模型的文字輸入。包含詳細指示、問題或上下文。（預設：""） | STRING | 是 | 必須包含至少一個非空白字元 |
| `seed` | 取樣用的種子。設為 0 可取得隨機種子。不保證輸出具有確定性。（預設：42） | INT | 是 | 0 到 2147483647 |
| `system_prompt` | 決定模型行為的基礎指示。（預設：""） | STRING | 否 |  |

### Gemini 3.8 Flash 輸入

當 `model` 設為 `"Gemini 3.8 Flash"` 時，會顯示這些輸入。

| 參數 | 描述 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前進行內部推理的程度。HIGH 可提升困難任務的品質，但會耗用更多（思考）token 且速度較慢。（預設："MEDIUM"） | COMBO | 是 | `"LOW"`<br>`"MEDIUM"`<br>`"HIGH"` |
| `max_output_tokens` | 要生成的最大 token 數，包含模型的內部思考。當 `thinking_level` 為 `HIGH` 時，較低的值可能沒有空間容納答案；若回應為空或被截斷，請提高此值。模型完成後會提前停止，因此對於簡短回覆，較高的上限不會產生額外成本。（預設：32768） | INT | 是 | 16 到 65536 |

**注意：** 此模型不提供 `temperature` 或 `top_p` 取樣控制。

### Gemini 3.7 Flash 輸入

當 `model` 設為 `"Gemini 3.7 Flash"` 時，會顯示這些輸入。

| 參數 | 描述 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前進行內部推理的程度。HIGH 可提升困難任務的品質，但會耗用更多（思考）token 且速度較慢。（預設："MEDIUM"） | COMBO | 是 | `"LOW"`<br>`"MEDIUM"`<br>`"HIGH"` |
| `temperature` | 控制隨機性。較低值較專注/確定，較高值較有創意。（預設：1.0） | FLOAT | 是 | 0.0 到 2.0 |
| `top_p` | 核取樣：從累積機率達到 `top_p` 的最小 token 集合中取樣。（預設：0.95） | FLOAT | 是 | 0.0 到 1.0 |
| `max_output_tokens` | 要生成的最大 token 數，包含模型的內部思考。當 `thinking_level` 為 `HIGH` 時，較低的值可能沒有空間容納答案；若回應為空或被截斷，請提高此值。模型完成後會提前停止，因此對於簡短回覆，較高的上限不會產生額外成本。（預設：32768） | INT | 是 | 16 到 65536 |

### Gemini 3.5 Flash 輸入

當 `model` 設為 `"Gemini 3.5 Flash"` 時，會顯示這些輸入。

| 參數 | 描述 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前進行內部推理的程度。HIGH 可提升困難任務的品質，但會耗用更多（思考）token 且速度較慢。（預設："MEDIUM"） | COMBO | 是 | `"MINIMAL"`<br>`"LOW"`<br>`"MEDIUM"`<br>`"HIGH"` |
| `temperature` | 控制隨機性。較低值較專注/確定，較高值較有創意。（預設：1.0） | FLOAT | 是 | 0.0 到 2.0 |
| `top_p` | 核取樣：從累積機率達到 `top_p` 的最小 token 集合中取樣。（預設：0.95） | FLOAT | 是 | 0.0 到 1.0 |
| `max_output_tokens` | 要生成的最大 token 數，包含模型的內部思考。當 `thinking_level` 為 `HIGH` 時，較低的值可能沒有空間容納答案；若回應為空或被截斷，請提高此值。模型完成後會提前停止，因此對於簡短回覆，較高的上限不會產生額外成本。（預設：32768） | INT | 是 | 16 到 65536 |

### Gemini 3.1 Pro 輸入

當 `model` 設為 `"Gemini 3.1 Pro"` 時，會顯示這些輸入。

| 參數 | 描述 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前進行內部推理的程度。HIGH 可提升困難任務的品質，但會耗用更多（思考）token 且速度較慢。（預設："HIGH"） | COMBO | 是 | `"LOW"`<br>`"HIGH"` |
| `temperature` | 控制隨機性。較低值較專注/確定，較高值較有創意。（預設：1.0） | FLOAT | 是 | 0.0 到 2.0 |
| `top_p` | 核取樣：從累積機率達到 `top_p` 的最小 token 集合中取樣。（預設：0.95） | FLOAT | 是 | 0.0 到 1.0 |
| `max_output_tokens` | 要生成的最大 token 數，包含模型的內部思考。當 `thinking_level` 為 `HIGH` 時，較低的值可能沒有空間容納答案；若回應為空或被截斷，請提高此值。模型完成後會提前停止，因此對於簡短回覆，較高的上限不會產生額外成本。（預設：32768） | INT | 是 | 16 到 65536 |

### Gemini 3.1 Flash-Lite 輸入

當 `model` 設為 `"Gemini 3.1 Flash-Lite"` 時，會顯示這些輸入。

| 參數 | 描述 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前進行內部推理的程度。HIGH 可提升困難任務的品質，但會耗用更多（思考）token 且速度較慢。（預設："LOW"） | COMBO | 是 | `"LOW"`<br>`"HIGH"` |
| `temperature` | 控制隨機性。較低值較專注/確定，較高值較有創意。（預設：1.0） | FLOAT | 是 | 0.0 到 2.0 |
| `top_p` | 核取樣：從累積機率達到 `top_p` 的最小 token 集合中取樣。（預設：0.95） | FLOAT | 是 | 0.0 到 1.0 |
| `max_output_tokens` | 要生成的最大 token 數，包含模型的內部思考。當 `thinking_level` 為 `HIGH` 時，較低的值可能沒有空間容納答案；若回應為空或被截斷，請提高此值。模型完成後會提前停止，因此對於簡短回覆，較高的上限不會產生額外成本。（預設：32768） | INT | 是 | 16 到 65536 |

### 媒體與檔案輸入

以下輸入由所有模型共用，並與各模型專屬輸入一同顯示。

| 參數 | 描述 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `images` | 可擴充插槽：連接 1 到 16 張影像（`image_1` ... `image_16`）。可選的影像，可作為模型的上下文。最多 16 張影像。 | IMAGE | 否 | 0 到 16 張影像 |
| `audio` | 可擴充插槽：連接一個音訊片段（`audio_1`）。可選的音訊片段，可作為模型的上下文。 | AUDIO | 否 | 0 到 1 個片段 |
| `video` | 可擴充插槽：連接一個影片片段（`video_1`）。可選的影片片段，可作為模型的上下文。 | VIDEO | 否 | 0 到 1 個片段 |
| `files` | 可選的檔案，可作為模型的上下文。接受來自 Gemini Input Files 節點的輸入。 | GEMINI_INPUT_FILES | 否 |  |

**注意：** 當附加媒體（影像、音訊或影片）時，節點會將前 10 個媒體項目上傳至 ComfyAPI 儲存空間，並以 URL 傳遞；此 URL 額度由所有媒體類型共用，並依序消耗（影片優先，接著音訊，最後影像）。任何剩餘媒體會以 base64 資料內嵌編碼，內嵌總酬載上限為 18 MB。若內嵌酬載會超過 18 MB，節點會拋出錯誤。`prompt` 參數必須包含至少一個非空白字元。將 `seed` 設為 0 會要求隨機種子。

## 輸出

| 輸出名稱 | 描述 | 資料類型 |
|-------------|-------------|-----------|
| `output` | 從 Gemini 模型生成的文字回應。如果模型未產生任何文字，則會傳回字串 "Empty response from Gemini model..."。 | STRING |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/GeminiNodeV2/zh-TW.md)

---
**Source fingerprint (SHA-256):** `8ae14c6465569695e1e99b0040cb2c745e5f3d9ddcd3b03013530dc7e7135b87`
