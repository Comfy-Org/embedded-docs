# Google Gemini

使用 Google 的 Gemini 模型產生文字回應。提供文字提示，並可選擇性提供一或多張圖像、音訊片段、影片或檔案作為多模態上下文。

## 輸入

### 通用輸入

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `model` | 用於產生回應的 Gemini 模型。 | DYNAMIC_COMBO | 是 | `"Gemini 3.8 Flash"`<br>`"Gemini 3.7 Flash"`<br>`"Gemini 3.5 Flash"`<br>`"Gemini 3.1 Pro"`<br>`"Gemini 3.1 Flash-Lite"` |
| `prompt` | 提供給模型的文字輸入。請包含詳細指示、問題或上下文。必須包含至少一個非空白字元。（預設值：""） | STRING | 是 |  |
| `seed` | 用於取樣的種子。設為 0 表示使用隨機種子。不保證輸出具有確定性。（預設值：42） | INT | 是 | 0 至 2147483647 |
| `system_prompt` | 用於規範模型行為的基礎指示。（預設值：""） | STRING | 否 |  |

### Gemini 3.8 Flash 輸入

當 `model` 設為 `"Gemini 3.8 Flash"` 時，會顯示這些輸入。

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前於內部進行推理的用力程度。HIGH 可提升困難任務的品質，但會耗費更多（思考）token 且速度較慢。（預設值："MEDIUM"） | COMBO | 是 | `"LOW"`<br>`"MEDIUM"`<br>`"HIGH"` |
| `max_output_tokens` | 要產生的最大 token 數，包含模型的內部思考。當 `thinking_level` 為 HIGH 時，數值太低可能沒有空間產生答案；若回應為空或被截斷，請提高此值。模型完成後會提早停止，因此對簡短回覆而言，設定較高的上限不會產生額外成本。（預設值：32768） | INT | 是 | 16 至 65536 |

**注意：** 此模型不提供 `temperature` 或 `top_p` 取樣控制。

### Gemini 3.7 Flash 輸入

當 `model` 設為 `"Gemini 3.7 Flash"` 時，會顯示這些輸入。

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前於內部進行推理的用力程度。HIGH 可提升困難任務的品質，但會耗費更多（思考）token 且速度較慢。（預設值："MEDIUM"） | COMBO | 是 | `"LOW"`<br>`"MEDIUM"`<br>`"HIGH"` |
| `temperature` | 控制隨機性。數值越低，輸出越聚焦/確定；數值越高，越有創意。（預設值：1.0） | FLOAT | 是 | 0.0 至 2.0 |
| `top_p` | 核心取樣：從累積機率達到 `top_p` 的最小 token 集合中取樣。（預設值：0.95） | FLOAT | 是 | 0.0 至 1.0 |
| `max_output_tokens` | 要產生的最大 token 數，包含模型的內部思考。當 `thinking_level` 為 HIGH 時，數值太低可能沒有空間產生答案；若回應為空或被截斷，請提高此值。模型完成後會提早停止，因此對簡短回覆而言，設定較高的上限不會產生額外成本。（預設值：32768） | INT | 是 | 16 至 65536 |

### Gemini 3.5 Flash 輸入

當 `model` 設為 `"Gemini 3.5 Flash"` 時，會顯示這些輸入。

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前於內部進行推理的用力程度。HIGH 可提升困難任務的品質，但會耗費更多（思考）token 且速度較慢。（預設值："MEDIUM"） | COMBO | 是 | `"MINIMAL"`<br>`"LOW"`<br>`"MEDIUM"`<br>`"HIGH"` |
| `temperature` | 控制隨機性。數值越低，輸出越聚焦/確定；數值越高，越有創意。（預設值：1.0） | FLOAT | 是 | 0.0 至 2.0 |
| `top_p` | 核心取樣：從累積機率達到 `top_p` 的最小 token 集合中取樣。（預設值：0.95） | FLOAT | 是 | 0.0 至 1.0 |
| `max_output_tokens` | 要產生的最大 token 數，包含模型的內部思考。當 `thinking_level` 為 HIGH 時，數值太低可能沒有空間產生答案；若回應為空或被截斷，請提高此值。模型完成後會提早停止，因此對簡短回覆而言，設定較高的上限不會產生額外成本。（預設值：32768） | INT | 是 | 16 至 65536 |

### Gemini 3.1 Pro 輸入

當 `model` 設為 `"Gemini 3.1 Pro"` 時，會顯示這些輸入。

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前於內部進行推理的用力程度。HIGH 可提升困難任務的品質，但會耗費更多（思考）token 且速度較慢。（預設值："HIGH"） | COMBO | 是 | `"LOW"`<br>`"HIGH"` |
| `temperature` | 控制隨機性。數值越低，輸出越聚焦/確定；數值越高，越有創意。（預設值：1.0） | FLOAT | 是 | 0.0 至 2.0 |
| `top_p` | 核心取樣：從累積機率達到 `top_p` 的最小 token 集合中取樣。（預設值：0.95） | FLOAT | 是 | 0.0 至 1.0 |
| `max_output_tokens` | 要產生的最大 token 數，包含模型的內部思考。當 `thinking_level` 為 HIGH 時，數值太低可能沒有空間產生答案；若回應為空或被截斷，請提高此值。模型完成後會提早停止，因此對簡短回覆而言，設定較高的上限不會產生額外成本。（預設值：32768） | INT | 是 | 16 至 65536 |

### Gemini 3.1 Flash-Lite 輸入

當 `model` 設為 `"Gemini 3.1 Flash-Lite"` 時，會顯示這些輸入。

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `thinking_level` | 模型在回答前於內部進行推理的用力程度。HIGH 可提升困難任務的品質，但會耗費更多（思考）token 且速度較慢。（預設值："LOW"） | COMBO | 是 | `"LOW"`<br>`"HIGH"` |
| `temperature` | 控制隨機性。數值越低，輸出越聚焦/確定；數值越高，越有創意。（預設值：1.0） | FLOAT | 是 | 0.0 至 2.0 |
| `top_p` | 核心取樣：從累積機率達到 `top_p` 的最小 token 集合中取樣。（預設值：0.95） | FLOAT | 是 | 0.0 至 1.0 |
| `max_output_tokens` | 要產生的最大 token 數，包含模型的內部思考。當 `thinking_level` 為 HIGH 時，數值太低可能沒有空間產生答案；若回應為空或被截斷，請提高此值。模型完成後會提早停止，因此對簡短回覆而言，設定較高的上限不會產生額外成本。（預設值：32768） | INT | 是 | 16 至 65536 |

### 媒體與檔案輸入

以下輸入由所有模型共用，並會與各模型專屬的輸入一同顯示。

| 參數 | 說明 | 資料類型 | 必填 | 範圍 |
|-----------|-------------|-----------|----------|-------|
| `images` | 可擴充插槽：連接 1 到 16 張圖像（`image_1` ... `image_16`）。可選擇性提供一或多張圖像作為模型的上下文。最多 16 張圖像。 | IMAGE | 否 | 0 至 16 images |
| `audio` | 可擴充插槽：連接一個音訊片段（`audio_1`）。可選擇性提供一個音訊片段作為模型的上下文。 | AUDIO | 否 | 0 至 1 clip |
| `video` | 可擴充插槽：連接一個影片片段（`video_1`）。可選擇性提供一個影片片段作為模型的上下文。 | VIDEO | 否 | 0 至 1 clip |
| `files` | 可選擇性提供一或多個檔案作為模型的上下文。接受來自 Gemini Input Files 節點的輸入。 | GEMINI_INPUT_FILES | 否 |  |

**注意：** 當附加媒體（圖像、音訊或影片）時，節點會將前 10 個媒體項目上傳至 ComfyAPI 儲存空間，並以 URL 形式傳遞；此 URL 額度由所有媒體類型共用，並依序消耗（先影片，再音訊，最後圖像）。任何剩餘媒體會以 base64 資料內嵌編碼，內嵌內容負載合計上限為 18 MB。若內嵌內容負載超過 18 MB，節點會拋出錯誤。`prompt` 參數必須包含至少一個非空白字元。將 `seed` 設為 0 會要求使用隨機種子。

## 輸出

| 輸出名稱 | 說明 | 資料類型 |
|-------------|-------------|-----------|
| `output` | 由 Gemini 模型產生的文字回應。若模型沒有產生任何文字，則會傳回字串 "Empty response from Gemini model..."。 | STRING |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/GeminiNodeV2/zh-TW.md)

---
**Source fingerprint (SHA-256):** `98a19d1b29e80907477d24d813593950a028021ee8bcf634f505e11a15daa383`
