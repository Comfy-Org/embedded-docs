> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/PatchModelAddDownscale/fa.md)

گره PatchModelAddDownscale عملکرد Kohya Deep Shrink را با اعمال عملیات کاهش و افزایش مقیاس بر روی بلوک‌های خاصی در یک مدل پیاده‌سازی می‌کند. این گره وضوح ویژگی‌های میانی را در طول پردازش کاهش داده و سپس آن‌ها را به اندازه اصلی بازمی‌گرداند که می‌تواند عملکرد را بهبود بخشد و در عین حال کیفیت را حفظ کند. این گره امکان کنترل دقیق بر زمان و نحوه انجام این عملیات مقیاس‌بندی را در طول اجرای مدل فراهم می‌کند.

## ورودی‌ها

| پارامتر | نوع داده | اجباری | محدوده | توضیحات |
|---------|----------|--------|--------|---------|
| `مدل` | MODEL | بله | - | مدلی که پچ کاهش مقیاس روی آن اعمال می‌شود |
| `شماره بلاک` | INT | خیر | 1-32 | شماره بلوک خاصی که کاهش مقیاس روی آن اعمال می‌شود (پیش‌فرض: 3) |
| `ضریب کاهش ابعاد` | FLOAT | خیر | 0.1-9.0 | ضریب کاهش مقیاس ویژگی‌ها (پیش‌فرض: 2.0) |
| `درصد شروع` | FLOAT | خیر | 0.0-1.0 | نقطه شروع در فرآیند نویززدایی که کاهش مقیاس از آنجا آغاز می‌شود (پیش‌فرض: 0.0) |
| `درصد پایان` | FLOAT | خیر | 0.0-1.0 | نقطه پایان در فرآیند نویززدایی که کاهش مقیاس در آن متوقف می‌شود (پیش‌فرض: 0.35) |
| `کاهش ابعاد پس از پرش` | BOOLEAN | خیر | - | آیا کاهش مقیاس پس از اتصالات پرشی اعمال شود (پیش‌فرض: True) |
| `روش کاهش ابعاد` | COMBO | خیر | "bicubic"<br>"nearest-exact"<br>"bilinear"<br>"area"<br>"bislerp" | روش درون‌یابی مورد استفاده برای عملیات کاهش مقیاس |
| `روش افزایش ابعاد` | COMBO | خیر | "bicubic"<br>"nearest-exact"<br>"bilinear"<br>"area"<br>"bislerp" | روش درون‌یابی مورد استفاده برای عملیات افزایش مقیاس |

## خروجی‌ها

| نام خروجی | نوع داده | توضیحات |
|-----------|----------|---------|
| `مدل` | MODEL | مدل اصلاح‌شده با پچ کاهش مقیاس اعمال‌شده |

---
**Source fingerprint (SHA-256):** `93ece77ad2dce3c1cdd554583ae1f2e6be51a43ab072d408869dddbcc7798c40`
