# WanMoveTracksFromCoords

Узел WanMoveTracksFromCoords создаёт треки движения из строки координат в формате JSON. Он преобразует данные координат в формат треков, который может использоваться другими узлами обработки видео, и при необходимости может применять маску для управления видимостью треков во времени.

## Входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `track_coords` | Строка в формате JSON, содержащая данные координат для треков. Значение по умолчанию — пустой список (`"[]"`). Это принудительный вход, поэтому его необходимо подключить в интерфейсе. | STRING | Нет | N/A |
| `track_mask` | Необязательная маска. Если она указана, узел использует её для определения видимости треков по кадрам: треки видимы в кадрах, где маска содержит хотя бы один ненулевой пиксель. Если не указана, все треки видимы во всех кадрах. | MASK | Нет | N/A |

**Примечание:** Вход `track_coords` ожидает определённую структуру JSON. Это должен быть список треков, где каждый трек — список кадров, а каждый кадр — объект с координатами `x` и `y`. Количество кадров должно быть одинаковым для всех треков, и должен быть предоставлен как минимум один трек.

## Выходы

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `tracks` | Сгенерированные данные треков, содержащие координаты пути и информацию о видимости для каждого трека. | TRACKS |
| `track_length` | Общее количество кадров в сгенерированных треках. | INT |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/WanMoveTracksFromCoords/ru.md)

---
**Source fingerprint (SHA-256):** `125187c96332fa81f0a30bcc4c927f405b56b578638ea565642a2b88dff808b7`
