# حذف پس‌زمینه ویدیو Bria

این گره پس‌زمینه را از یک ویدیو با استفاده از سرویس Bria AI حذف می‌کند. ویدیوی ورودی را پردازش کرده و پس‌زمینه اصلی را با یک رنگ یکدست به انتخاب شما جایگزین می‌کند. این عملیات از طریق یک API خارجی انجام می‌شود و نتیجه به‌صورت یک فایل ویدیویی جدید بازگردانده می‌شود.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
|-----------|-------------|-----------|----------|-------|
| `video` | فایل ویدیوی ورودی که پس‌زمینه از آن حذف می‌شود. | VIDEO | بله | N/A |
| `background_color` | رنگ پس‌زمینه برای ویدیوی خروجی. | COMBO | بله | `"Black"`<br>`"White"`<br>`"Gray"`<br>`"Red"`<br>`"Green"`<br>`"Blue"`<br>`"Yellow"`<br>`"Cyan"`<br>`"Magenta"`<br>`"Orange"` |
| `seed` | seed کنترل می‌کند که آیا گره باید دوباره اجرا شود؛ نتایج بدون توجه به seed غیرقطعی هستند. (پیش‌فرض: 0) | INT | بله | 0 تا 2147483647 |

**توجه:** ویدیوی ورودی باید مدت‌زمان ۶۰ ثانیه یا کمتر داشته باشد. نتایج حتی اگر مقدار seed تغییر نکند، غیرقطعی هستند.

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
|-------------|-------------|-----------|
| `output` | فایل ویدیویی پردازش‌شده با پس‌زمینه حذف‌شده و جایگزین‌شده با رنگ انتخاب‌شده، که به‌صورت MP4 با کدک H.264 کدگذاری شده است. | VIDEO |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/BriaRemoveVideoBackground/fa.md)

---
**Source fingerprint (SHA-256):** `dbd6b7393f893be5a40322fc96b90bb3d5f1818bdda7b8109b28f48baac44d59`
