# Empty HiDream-O1 潛空間圖像

此節點會在像素空間中為 HiDream-O1-Image 模型建立一個空的潛在影像。它會產生一個由零填充的空白張量，作為影像生成的起始點，其尺寸由 `width`、`height` 與 `batch_size` 輸入定義。

## 輸入

| 參數 | 描述 | 資料類型 | 必填 | 範圍 |
| --- | --- | --- | --- | --- |
| `寬度` | 潛在影像的寬度，以像素為單位。預設值：2048。此值必須為 32 的倍數。模型是在約 400 萬像素的解析度下訓練；較低的解析度可能會明顯降低影像品質。 | INT | 是 | 64 至 4096 （步進值：32） |
| `高度` | 潛在影像的高度，以像素為單位。預設值：2048。此值必須為 32 的倍數。模型是在約 400 萬像素的解析度下訓練；較低的解析度可能會明顯降低影像品質。 | INT | 是 | 64 至 4096 （步進值：32） |
| `批次大小` | 單一批次中要生成的潛在影像數量。預設值：1。 | INT | 是 | 1 至 64 |

## 輸出

| 輸出名稱 | 描述 | 資料類型 |
| --- | --- | --- |
| `samples` | 一個填滿零的張量，代表空的潛在影像，其形狀為 (batch_size, 3, height, width)。 | LATENT |

## 備註

- HiDream-O1-Image 模型是在約 400 萬像素下訓練。使用明顯較低的解析度可能會導致影像品質明顯下降。
- 訓練使用的解析度包括：2048x2048, 2304x1728, 1728x2304, 2560x1440, 1440x2560, 2496x1664, 1664x2496, 3104x1312, 1312x3104, 2304x1792, 1792x2304。

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/EmptyHiDreamO1LatentImage/zh-TW.md)

---
**Source fingerprint (SHA-256):** `7412639e261512d9174e60009143c8c06c354e2a20ada7271837d72053426be5`
