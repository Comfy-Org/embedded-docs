> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/CLIPTextEncodeSDXL/ru.md)

Этот узел предназначен для кодирования текстового ввода с помощью модели CLIP, специально адаптированной для архитектуры SDXL. Он использует двойную систему кодировщиков (CLIP-L и CLIP-G) для обработки текстовых описаний, что обеспечивает более точную генерацию изображений.

## Входные параметры

| Параметр | Тип данных | Описание |
|-----------|-----------|-------------|
| `clip` | CLIP | Экземпляр модели CLIP, используемый для кодирования текста. |
| `ширина` | INT | Задает ширину изображения в пикселях, по умолчанию 1024. |
| `высота` | INT | Задает высоту изображения в пикселях, по умолчанию 1024. |
| `обрезка_ширины` | INT | Ширина области кадрирования в пикселях, по умолчанию 0. |
| `обрезка_высоты` | INT | Высота области кадрирования в пикселях, по умолчанию 0. |
| `целевая_ширина` | INT | Целевая ширина выходного изображения, по умолчанию 1024. |
| `целевая_высота` | INT | Целевая высота выходного изображения, по умолчанию 1024. |
| `текст_g` | STRING | Глобальное текстовое описание для общего описания сцены. |
| `текст_l` | STRING | Локальное текстовое описание для детального описания. |

## Выходные параметры

| Параметр | Тип данных | Описание |
|-----------|-----------|-------------|
| `CONDITIONING` | CONDITIONING | Содержит закодированный текст и условную информацию, необходимую для генерации изображения. |