> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/WanInfiniteTalkToVideo/fa.md)

### **گره WanInfiniteTalkToVideo**

گره WanInfiniteTalkToVideo توالی‌های ویدئویی را از ورودی صوتی تولید می‌کند. این گره از یک مدل انتشار ویدئو (video diffusion model) استفاده می‌کند که با ویژگی‌های صوتی استخراج‌شده از یک یا دو گوینده، شرطی شده است تا یک نمایش نهفته (latent representation) از ویدئوی چهره‌ی سخنگو تولید کند. این گره می‌تواند یک توالی جدید ایجاد کرده یا با استفاده از فریم‌های قبلی برای زمینه‌ی حرکتی، یک توالی موجود را گسترش دهد.

## **ورودی‌ها**

| پارامتر | نوع داده | اجباری | محدوده | توضیحات |
|-----------|-----------|----------|-------|-------------|
| `حالت` | COMBO | بله | `"single_speaker"`<br>`"two_speakers"` | حالت ورودی صوتی. `"single_speaker"` از یک ورودی صوتی استفاده می‌کند. `"two_speakers"` ورودی‌هایی را برای گوینده دوم و ماسک‌های مربوطه فعال می‌کند. |
| `مدل` | MODEL | بله | - | مدل پایه انتشار ویدئو. |
| `patch مدل` | MODELPATCH | بله | - | وصله مدل (model patch) حاوی لایه‌های فرافکنی صوتی. |
| `مثبت` | CONDITIONING | بله | - | شرطی‌سازی مثبت برای هدایت تولید. |
| `منفی` | CONDITIONING | بله | - | شرطی‌سازی منفی برای هدایت تولید. |
| `vae` | VAE | بله | - | VAE مورد استفاده برای رمزگذاری تصاویر به فضای نهفته و رمزگشایی از آن. |
| `عرض` | INT | خیر | 16 - MAX_RESOLUTION | عرض ویدئوی خروجی بر حسب پیکسل. باید بر 16 بخش‌پذیر باشد. (پیش‌فرض: 832) |
| `ارتفاع` | INT | خیر | 16 - MAX_RESOLUTION | ارتفاع ویدئوی خروجی بر حسب پیکسل. باید بر 16 بخش‌پذیر باشد. (پیش‌فرض: 480) |
| `طول` | INT | خیر | 1 - MAX_RESOLUTION | تعداد فریم‌های تولیدی. (پیش‌فرض: 81) |
| `خروجی بینایی clip` | CLIPVISIONOUTPUT | خیر | - | خروجی بینایی CLIP اختیاری برای شرطی‌سازی اضافی. |
| `تصویر شروع` | IMAGE | خیر | - | یک تصویر شروع اختیاری برای مقداردهی اولیه توالی ویدئو. |
| `خروجی رمزگذار صوتی ۱` | AUDIOENCODEROUTPUT | بله | - | خروجی رمزگذار صوتی اصلی حاوی ویژگی‌های گوینده اول. |
| `تعداد فریم‌های حرکتی` | INT | خیر | 1 - 33 | تعداد فریم‌های قبلی که هنگام گسترش یک توالی به عنوان زمینه حرکتی استفاده می‌شود. (پیش‌فرض: 9) |
| `مقیاس صوتی` | FLOAT | خیر | 10.0 - 10.0- | یک عامل مقیاس‌دهی که به شرطی‌سازی صوتی اعمال می‌شود. (پیش‌فرض: 1.0) |
| `فریم‌های قبلی` | IMAGE | خیر | - | فریم‌های ویدئویی قبلی اختیاری برای گسترش از آن‌ها. |
| `audio_encoder_output_2` | AUDIOENCODEROUTPUT | خیر | - | خروجی رمزگذار صوتی دوم. هنگامی که `حالت` روی `"two_speakers"` تنظیم شده باشد، اجباری است. |
| `mask_1` | MASK | خیر | - | ماسک گوینده اول، در صورت استفاده از دو ورودی صوتی اجباری است. |
| `mask_2` | MASK | خیر | - | ماسک گوینده دوم، در صورت استفاده از دو ورودی صوتی اجباری است. |

**محدودیت‌های پارامتر:**

* هنگامی که `mode` روی `"two_speakers"` تنظیم شده باشد، پارامترهای `audio_encoder_output_2`، `mask_1` و `mask_2` اجباری می‌شوند.
* اگر `audio_encoder_output_2` ارائه شود، هر دو `mask_1` و `mask_2` نیز باید ارائه شوند.
* اگر `mask_1` و `mask_2` ارائه شوند، `audio_encoder_output_2` نیز باید ارائه شود.
* اگر `previous_frames` ارائه شود، باید حداقل به تعداد مشخص‌شده توسط `motion_frame_count` فریم داشته باشد.

## **خروجی‌ها**

| نام خروجی | نوع داده | توضیحات |
|-------------|-----------|-------------|
| `مدل` | MODEL | مدل وصله‌شده با شرطی‌سازی صوتی اعمال‌شده. |
| `مثبت` | CONDITIONING | شرطی‌سازی مثبت، که احتمالاً با زمینه اضافی (مانند تصویر شروع، بینایی CLIP) اصلاح شده است. |
| `منفی` | CONDITIONING | شرطی‌سازی منفی، که احتمالاً با زمینه اضافی اصلاح شده است. |
| `latent` | LATENT | توالی ویدئوی تولیدشده در فضای نهفته. |
| `trim_image` | INT | تعداد فریم‌هایی از ابتدای زمینه حرکتی که هنگام گسترش یک توالی باید حذف شوند. |

---
**Source fingerprint (SHA-256):** `6bb976da5cac0b61edb7d4c9d206c7c7ea9ffc0e982034c23c7f2e891e972888`
