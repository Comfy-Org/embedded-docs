# ModelMergeSD35_Large

گره `ModelMergeSD35_Large` دو مدل Stable Diffusion 3.5 Large را با ترکیب مؤلفه‌های داخلی خاصی از مدل دوم در مدل اول ادغام می‌کند. هر مؤلفه مقدار ترکیب مستقلی دارد که کنترل دقیقی بر میزان مشارکت آن مؤلفه در مدل ادغام‌شدهٔ نهایی فراهم می‌کند.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
| --- | --- | --- | --- | --- |
| `مدل۱` | مدل پایه‌ای که به‌عنوان پایهٔ ادغام عمل می‌کند | MODEL | بله | - |
| `مدل۲` | مدل دومی که مؤلفه‌های آن در مدل پایه ترکیب می‌شوند | MODEL | بله | - |
| `pos_embed.` | میزان جاسازی موقعیت از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `x_embedder.` | میزان جاسازندهٔ x از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `context_embedder.` | میزان جاسازندهٔ بافت (context) از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `y_embedder.` | میزان جاسازندهٔ y از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `t_embedder.` | میزان جاسازندهٔ t از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.0.` | میزان بلوک مشترک 0 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.1.` | میزان بلوک مشترک 1 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.2.` | میزان بلوک مشترک 2 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.3.` | میزان بلوک مشترک 3 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.4.` | میزان بلوک مشترک 4 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.5.` | میزان بلوک مشترک 5 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.6.` | میزان بلوک مشترک 6 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.7.` | میزان بلوک مشترک 7 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.8.` | میزان بلوک مشترک 8 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.9.` | میزان بلوک مشترک 9 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.10.` | میزان بلوک مشترک 10 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.11.` | میزان بلوک مشترک 11 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.12.` | میزان بلوک مشترک 12 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.13.` | میزان بلوک مشترک 13 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.14.` | میزان بلوک مشترک 14 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.15.` | میزان بلوک مشترک 15 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.16.` | میزان بلوک مشترک 16 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.17.` | میزان بلوک مشترک 17 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.18.` | میزان بلوک مشترک 18 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.19.` | میزان بلوک مشترک 19 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.20.` | میزان بلوک مشترک 20 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.21.` | میزان بلوک مشترک 21 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.22.` | میزان بلوک مشترک 22 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.23.` | میزان بلوک مشترک 23 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.24.` | میزان بلوک مشترک 24 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.25.` | میزان بلوک مشترک 25 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.26.` | میزان بلوک مشترک 26 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.27.` | میزان بلوک مشترک 27 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.28.` | میزان بلوک مشترک 28 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.29.` | میزان بلوک مشترک 29 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.30.` | میزان بلوک مشترک 30 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.31.` | میزان بلوک مشترک 31 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.32.` | میزان بلوک مشترک 32 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.33.` | میزان بلوک مشترک 33 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.34.` | میزان بلوک مشترک 34 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.35.` | میزان بلوک مشترک 35 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.36.` | میزان بلوک مشترک 36 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `joint_blocks.37.` | میزان بلوک مشترک 37 از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |
| `final_layer.` | میزان لایهٔ نهایی از `model2` را که در مدل ادغام‌شده ترکیب می‌شود کنترل می‌کند (پیش‌فرض: 1.0) | FLOAT | بله | 0.0 تا 1.0 |

**توجه:** همهٔ پارامترهای ترکیب، مقادیر 0.0 تا 1.0 را با گام 0.01 می‌پذیرند. مقدار 0.0 به معنای عدم مشارکت از `model2` و مقدار 1.0 به معنای مشارکت کامل از `model2` برای آن مؤلفهٔ خاص است.

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
| --- | --- | --- |
| `model` | مدل ادغام‌شدهٔ نهایی که ویژگی‌های هر دو مدل ورودی را بر اساس پارامترهای ترکیب مشخص‌شده ترکیب می‌کند | MODEL |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ModelMergeSD35_Large/fa.md)

---
**Source fingerprint (SHA-256):** `c489c710e18d01adcf4320d9c010ed587ca5e12babb468448f56d79acdc40f6c`
