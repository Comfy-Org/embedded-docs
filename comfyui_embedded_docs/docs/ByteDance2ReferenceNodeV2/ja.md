# ByteDance Seedance 2.5 参照から動画

ByteDance Seedance 2.5 Reference to Video は、テキストプロンプトとオプションの参照画像、動画、音声、または事前にアップロードされたライブラリアセットに導かれ、ByteDance Seedance モデル（Seedance 2.5、2.5 Draft、2.0、2.0 Fast、2.0 Mini）を使用して動画を生成、編集、または延長します。参照素材をアップロードし、生成タスクを送信し、完了を待って、完成した動画ファイルを返します。`Seedance 2.5 Draft` を選択すると、代わりに高速な 480p プレビューがレンダリングされます。生成された `draft_task_id` を ByteDance Seedance 2.5 Draft to Final Video ノードに接続すると、1080p の最終版をレンダリングできます。

## 入力

### 共通入力

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `モデル` | モデルセレクター。Seedance 2.5 は最新モデルで、最大 30 秒の動画と mp4/mov 出力に対応します。Seedance 2.5 Draft は高速な 480p プレビュー用で、その `draft_task_id` 出力を ByteDance Seedance 2.5 Draft to Final Video ノードに渡すと 1080p の最終版をレンダリングします。Seedance 2.0 は最高品質と 4k 用、Fast は速度最適化用、Mini は最速・最低コストの生成用です。モデルを選択すると、以下に表示される入力ウィジェットが変わります。 | DYNAMIC_COMBO | はい | "Seedance 2.5"<br>"Seedance 2.5 Draft"<br>"Seedance 2.0"<br>"Seedance 2.0 Fast"<br>"Seedance 2.0 Mini" |
| `シード` | `seed` はノードを再実行するかどうかを制御します。シードに関係なく結果は非決定的です。デフォルト: 0。 | INT | はい | 0～2147483647 |
| `ウォーターマーク` | 動画にウォーターマークを追加するかどうか。デフォルト: False。詳細設定。 | BOOLEAN | はい | true<br>false |

### Seedance 2.5 入力

これらの入力は、`model` が "Seedance 2.5" に設定されている場合に表示されます。

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `prompt` | 動画生成用のテキストプロンプト。生成されるセリフを誘導するには、発話行を二重引用符で囲みます。デフォルト: 空文字列。 | STRING | はい | 複数行テキスト |
| `resolution` | 出力動画の解像度。デフォルト: 720p。 | COMBO | はい | "480p"<br>"720p"<br>"1080p" |
| `ratio` | 出力動画のアスペクト比。デフォルト: 16:9。 | COMBO | はい | "16:9"<br>"4:3"<br>"1:1"<br>"3:4"<br>"9:16"<br>"21:9"<br>"adaptive" |
| `duration` | 出力動画の長さ（秒単位、4～30）。デフォルト: 5。 | INT | はい | 4～30 |
| `generate_audio` | 出力動画の音声生成を有効にします。デフォルト: True。 | BOOLEAN | はい | true<br>false |
| `task_type` | 参照メディアをどう扱うか。auto 以外の各値はタスク送信時に検証されるため、設定が一致しない場合は生成開始前に失敗します。<br>auto: モデルがプロンプトと入力からタスクを推論し、その解釈と矛盾する設定は生成開始後にのみ失敗します。<br>reference: 参照画像、動画、音声に導かれて新しい動画を生成します。<br>edit: 接続された参照動画を変更します（追加、削除、置換）。出力はソースクリップ自体の長さとアスペクト比を維持し、duration と ratio ウィジェットは無視されます。<br>extend: 接続された参照動画を前方または後方に続けます。プロンプトには「extend forward」「extend backward」「continue」のいずれかを指定する必要があり、アスペクト比はソースクリップに従い、出力には設定した duration の新しく生成されたセグメントのみが含まれ、ソースクリップは含まれません。デフォルト: auto。 | COMBO | はい | "auto"<br>"reference"<br>"edit"<br>"extend" |
| `output_format` | 出力動画のコンテナ形式。デフォルト: mp4。 | COMBO | はい | "mp4" |

### Seedance 2.5 Draft 入力

これらの入力は、`model` が "Seedance 2.5 Draft" に設定されている場合に表示されます。パラメータセットは上記の Seedance 2.5 と同じですが、`resolution` が `"480p"` のみを提供する点が異なります（デフォルト `"480p"`）。

### Seedance 2.0 入力

これらの入力は、`model` が "Seedance 2.0" に設定されている場合に表示されます。

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `prompt` | 動画生成用のテキストプロンプト。デフォルト: 空文字列。 | STRING | はい | 複数行テキスト |
| `resolution` | 出力動画の解像度。 | COMBO | はい | "480p"<br>"720p"<br>"1080p"<br>"4k" |
| `ratio` | 出力動画のアスペクト比。デフォルト: adaptive。 | COMBO | はい | "16:9"<br>"4:3"<br>"1:1"<br>"3:4"<br>"9:16"<br>"21:9"<br>"adaptive" |
| `duration` | 出力動画の長さ（秒単位、4～15）。デフォルト: 7。 | INT | はい | 4～15 |
| `generate_audio` | 出力動画の音声生成を有効にします。デフォルト: True。 | BOOLEAN | はい | true<br>false |

### Seedance 2.0 Fast および Seedance 2.0 Mini 入力

これらの入力は、`model` が "Seedance 2.0 Fast" または "Seedance 2.0 Mini" に設定されている場合に表示されます。両モデルは同じ入力セットを共有します。

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `prompt` | 動画生成用のテキストプロンプト。デフォルト: 空文字列。 | STRING | はい | 複数行テキスト |
| `resolution` | 出力動画の解像度。 | COMBO | はい | "480p"<br>"720p" |
| `ratio` | 出力動画のアスペクト比。デフォルト: adaptive。 | COMBO | はい | "16:9"<br>"4:3"<br>"1:1"<br>"3:4"<br>"9:16"<br>"21:9"<br>"adaptive" |
| `duration` | 出力動画の長さ（秒単位、4～15）。デフォルト: 7。 | INT | はい | 4～15 |
| `generate_audio` | 出力動画の音声生成を有効にします。デフォルト: True。 | BOOLEAN | はい | true<br>false |

### 参照入力

これらの拡張可能な参照スロットはすべてのモデルで利用できます。最大スロット数はモデルごとに異なります。Seedance 2.5 は最大 30 画像、10 動画、10 音声、30 アセットをサポートします。Seedance 2.0、2.0 Fast、2.0 Mini は最大 9 画像、3 動画、3 音声、9 アセットをサポートします。

| パラメータ | 説明 | データ型 | 必須 | 範囲 |
|-----------|-------------|-----------|----------|-------|
| `reference_images` | 拡張可能スロット: 出力を導く 1～N 個の参照画像を接続します。数の上限はモデルごとに異なります（モデル別セクションを参照）。画像はアスペクト比（0.4～2.5）が検証され、最大辺 6000 ピクセルに自動ダウンスケールされます。 | IMAGE | いいえ | 1～9 スロット（Seedance 2.0 ファミリー）<br>1～30 スロット（Seedance 2.5） |
| `reference_videos` | 拡張可能スロット: 1～N 個の参照動画を接続します。数の上限はモデルごとに異なります（モデル別セクションを参照）。各動画は少なくとも 1.8 秒の長さが必要で、選択したモデルと解像度のピクセル制限に収まる必要があります。 | VIDEO | いいえ | 1～3 スロット（Seedance 2.0 ファミリー）<br>1～10 スロット（Seedance 2.5） |
| `reference_audios` | 拡張可能スロット: 1～N 個の参照オーディオトラックを接続します。数の上限はモデルごとに異なります（モデル別セクションを参照）。各オーディオは少なくとも 1.8 秒の長さが必要です。 | AUDIO | いいえ | 1～3 スロット（Seedance 2.0 ファミリー）<br>1～10 スロット（Seedance 2.5） |
| `reference_assets` | 拡張可能スロット: Seedance 仮想ライブラリにすでにアップロードされているメディアのアセット ID 文字列を 1～N 個接続します。各アセットは Active である必要があります。プロンプト内で `asset1` や `asset 1` などのトークンでアセットを参照できます。ノードはそれらをアセットの位置ラベル（例: "Image 2" や "Video 1"）に置き換えます。 | STRING | いいえ | 1～9 スロット（Seedance 2.0 ファミリー）<br>1～30 スロット（Seedance 2.5） |
| `auto_downscale` | 選択した解像度でモデルのピクセルバジェットを超える参照動画を自動的にダウンスケールします。アスペクト比は維持され、すでに制限内の動画は変更されません。デフォルト: True。 | BOOLEAN | いいえ | true<br>false |
| `auto_upscale` | 選択した解像度でモデルの最小ピクセル数を下回る参照動画を自動的にアップスケールします。アスペクト比は維持され、すでに最小値を満たしている動画は変更されません。注: 低解像度ソースをアップスケールしても実際のディテールは追加されず、生成品質が低下する可能性があります。デフォルト: False。詳細設定。 | BOOLEAN | いいえ | true<br>false |

**注:** ノードを実行するには、少なくとも 1 つの参照画像、動画、またはアセットが必要です（Seedance 2.5 は音声のみの参照も受け付けます）。参照動画と参照オーディオはそれぞれ少なくとも 1.8 秒の長さが必要で、すべての参照動画の合計時間（および別途、すべての参照オーディオの合計時間）は、選択したモデルの最大合計秒数を超えてはなりません。参照画像は、アスペクト比がおおよそ 2:5～5:2（0.4～2.5）で、少なくとも 300x300 ピクセルである必要があり、最大辺 6000 ピクセルに自動ダウンスケールされます。`task_type` の "edit" および "extend" オプションは Seedance 2.5 でのみ利用でき、どちらも少なくとも 1 つの参照動画が必要です。"edit" を使用すると、出力はソースクリップ自体の長さとアスペクト比を維持し、`duration` と `ratio` ウィジェットは無視されます。"extend" を使用すると、出力には設定した duration の新しく生成されたセグメントのみが含まれます。参照アセットは Active ステータスである必要があり、そうでない場合タスクは失敗します。`draft_task_id` 出力は `Seedance 2.5 Draft` によってのみ生成されるため、他のモデルでは未接続のままにする必要があります。そうしないと実行が失敗します。

## 出力

| 出力名 | 説明 | データ型 |
|-------------|-------------|-----------|
| `video` | 生成された動画。生成タスクが完了すると、プロバイダーからダウンロードされます。音声生成が有効な場合は音声を含みます。 | VIDEO |
| `draft_task_id` | ドラフト実行のタスク ID。生成するのは Seedance 2.5 Draft モデルのみです。ByteDance Seedance 2.5 Draft to Final Video ノードに接続すると、1080p の最終版をレンダリングできます。 | STRING |

> このドキュメントは AI によって生成されました。エラーを見つけた場合や改善のご提案がある場合は、ぜひ貢献してください！ [GitHub で編集](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ByteDance2ReferenceNodeV2/ja.md)

---
**Source fingerprint (SHA-256):** `12fee29b280ff71e29f268f52131d1c15cf3066e0804356b735b61d97c80a6a9`
