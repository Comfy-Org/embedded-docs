> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [Edit on GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LatentComposite/zh-TW.md)

LatentComposite 節點專門用於將兩個潛在表徵混合或合併為單一輸出。此過程對於透過可控方式結合輸入潛在特徵來創建複合圖像或特徵至關重要。

## 輸入參數

| 參數名稱       | 資料類型    | 描述說明 |
|----------------|-------------|-------------|
| `目標樣本`   | `LATENT`    | 作為合成操作基礎的目標潛在表徵，`來源樣本` 將被合成到此基礎上 |
| `來源樣本` | `LATENT`    | 將被合成到 `目標樣本` 上的源潛在表徵，其特徵將貢獻於最終的複合輸出 |
| `X 座標`            | `INT`       | `來源樣本` 潛在表徵在 `目標樣本` 上放置的 X 座標（水平位置），決定合成的水平對齊方式 |
| `Y 座標`            | `INT`       | `來源樣本` 潛在表徵在 `目標樣本` 上放置的 Y 座標（垂直位置），決定合成的垂直對齊方式 |
| `羽化`      | `INT`       | 布林值，指示在合成前是否應將 `來源樣本` 潛在表徵調整大小以匹配 `目標樣本`，這會影響複合結果的尺度和比例 |

## 輸出結果

| 參數名稱 | 資料類型 | 描述說明 |
|----------|-------------|-------------|
| `latent` | `LATENT`    | 輸出的複合潛在表徵，根據指定座標和縮放選項混合了 `目標樣本` 和 `來源樣本` 兩者的特徵 |