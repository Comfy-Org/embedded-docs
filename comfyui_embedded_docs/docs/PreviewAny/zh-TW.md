# 預覽任意

PreviewAny 節點接受任何輸入值，並在介面中將其顯示為可讀文字。它旨在檢查和除錯工作流程中任何點的值：字串按原樣顯示，數字和布林值轉換為文字，其他物件則格式化為 JSON。轉換後的文字也會作為字串輸出傳遞，以便其他節點使用。

## 輸入

| 參數 | 描述 | 資料類型 | 必填 | 範圍 |
|------|------|---------|------|------|
| `source` | 要預覽為文字的值。接受任何資料類型。字串會原樣傳遞；數字和布林值轉換為文字；其他值序列化為帶縮排的 JSON。如果 JSON 序列化失敗，則使用該值的純字串表示；如果仍然失敗，則顯示文字「來源存在，但無法序列化。」 | ANY | 是 | Any data type |

## 輸出

| 輸出名 | 描述 | 資料類型 |
|--------|------|---------|
| `UI Text Display` | 在使用者介面中顯示轉換為文字的輸入資料。同一文字也會作為字串輸出返回，供其他節點進一步處理。 | STRING |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/PreviewAny/zh-TW.md)

---
**Source fingerprint (SHA-256):** `9572eac396aa27a83148a940e4bf37a9ec4e4a47e9526db8bb983611f9237536`
