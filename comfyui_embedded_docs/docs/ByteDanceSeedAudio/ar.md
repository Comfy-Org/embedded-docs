# ByteDanceSeedAudio

## نظرة عامة

قم بتوليد الكلام والموسيقى والمؤثرات الصوتية والحوار متعدد المتحدثين من موجه نصي واحد باستخدام ByteDance Seed Audio 1.0. صف الصوت (الأصوات)، والعاطفة، والأجواء، والموسيقى الخلفية، والمؤثرات الصوتية في الموجه، وقم بتضمين النص المراد نطقه. يمكنك اختيارياً اختيار صوت جاهز مدمج مسبقاً، أو استنساخ أصوات من ما يصل إلى 3 مقاطع مرجعية (موسومة بـ @Audio1-3 في الموجه)، أو اشتقاق صوت من صورة شخصية. مدة تصل إلى دقيقتين من الصوت لكل تشغيل.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
|---------|-------|---------------|--------|--------|
| `text_prompt` | صف الصوت (الأصوات)، والعاطفة، والإيقاع، والأجواء، والموسيقى الخلفية، والمؤثرات الصوتية، وقم بتضمين النص المراد نطقه (قم بتسمية الشخصيات في النص للحوار). في وضع "المرجع الصوتي"، أشر إلى المقاطع المتصلة حسب الترتيب كـ @Audio1، @Audio2، @Audio3. مع النموذج متعدد اللغات، يمكن أن يبدأ السطر المقتبس بنطاق طابع زمني يتحكم في متى ومدة نطقه، مثل `[5.5s:8.0s] انتظرني!`. اكتب الموجه بنفس لغة الأسطر المراد نطقها. الحد الأدنى حرف واحد، الحد الأقصى 3000 حرف. | STRING | نعم | 1 إلى 3000 حرف |
  - **"مرجع صوتي"**: يتطلب توصيل واحد على الأقل من `reference_audio_1` أو `reference_audio_2` أو `reference_audio_3`. يجب توصيل المقاطع المرجعية بالترتيب دون فجوات. يقتصر كل مقطع على 30 ثانية كحد أقصى. إذا تم استخدام وسوم @AudioN في الموجه، يجب ألا يتجاوز أعلى رقم وسم عدد المقاطع المرجعية المتصلة.
| `reference_audio_1` | مقطع مرجعي لاستنساخ الصوت، موسوم بـ @Audio1 في الموجه. مدة تصل إلى 30 ثانية. متاح فقط عندما يكون `reference_mode` هو "audio reference". | AUDIO | لا | حتى 30 ثانية |
| `reference_audio_2` | مقطع مرجعي موسوم بـ @Audio2 في الموجه. مدة تصل إلى 30 ثانية. متاح فقط عندما يكون `reference_mode` هو "audio reference". | AUDIO | لا | حتى 30 ثانية |
| `reference_audio_3` | مقطع مرجعي موسوم بـ @Audio3 في الموجه. مدة تصل إلى 30 ثانية. متاح فقط عندما يكون `reference_mode` هو "audio reference". | AUDIO | لا | حتى 30 ثانية |
  - **"مرجع صورة"**: يتطلب توصيل `reference_image`. لا تُستخدم وسوم @AudioN؛ يجب أن يحتوي الموجه على النص المراد تركيبه فقط.
  - **"صوت جاهز"**: يتطلب اختيار صوت محدد مسبقًا. يُقرأ الموجه بأكمله بالصوت المحدد؛ لا تُستخدم وسوم @AudioN كمراجع، ويتم رفض الوسوم مثل @Audio2 أو أعلى.
| `sample_rate` | معدل عينة الإخراج بالهرتز. (الافتراضي: "24000") | COMBO | نعم | `"8000"`<br>`"16000"`<br>`"24000"`<br>`"32000"`<br>`"44100"`<br>`"48000"` |
| `speech_rate` | سرعة الكلام. 0 = عادي، 100 = 2.0x، -50 = 0.5x. (الافتراضي: 0) | INT | نعم | -50 إلى 100 |
| `loudness_rate` | مستوى الصوت. 0 = عادي، 100 = 2.0x، -50 = 0.5x. (الافتراضي: 0) | INT | نعم | -50 إلى 100 |
| `pitch_rate` | تغيير طبقة الصوت بنصف نغمات (-12 إلى 12). (الافتراضي: 0) | INT | نعم | -12 إلى 12 |
| `seed` | البذرة تتحكم في ما إذا كان يجب إعادة تشغيل العقدة؛ النتائج غير حتمية بغض النظر عن البذرة. (الافتراضي: 42) | INT | نعم | 0 إلى 2147483647 |
| `model` | إصدار النموذج. يدعم `seed-audio-1.0-multilingual` 20 لغة والتحكم في التوقيت لكل جملة عبر الطوابع الزمنية `[5.5s:8.0s]`. يدعم `seed-audio-1.0` الإنجليزية والصينية فقط، بدون تحكم في التوقيت. (الافتراضي: "seed-audio-1.0-multilingual") | COMBO | لا | `"seed-audio-1.0-multilingual"`<br>`"seed-audio-1.0"` |

### قيود المعاملات

- **تبعيات وضع المرجع**: يحدد المعامل `reference_mode` المدخلات الإضافية المطلوبة:
  - **"نص فقط"**: لا حاجة لمدخلات إضافية. يجب ألا يحتوي الموجه على وسوم @AudioN.
  - **"مرجع صوتي"**: يتطلب توصيل واحد على الأقل من `reference_audio_1` أو `reference_audio_2` أو `reference_audio_3`. يجب توصيل المقاطع المرجعية بالترتيب دون فجوات (مثلاً _1، ثم _2، ثم _3). كل مقطع محدود بمدة أقصاها 30 ثانية. يجب أن يشير الموجه إلى المقاطع المتصلة باستخدام وسوم @Audio1 و @Audio2 و @Audio3.
  - **"مرجع صورة"**: يتطلب توصيل `reference_image`. لا تُستخدم وسوم @AudioN؛ يجب أن يحتوي الموجه على النص المراد تركيبه فقط.
  - **"صوت جاهز"**: يتطلب اختيار `preset_voice`. يجب ألا يحتوي الموجه على وسوم @AudioN (يُقرأ الموجه بأكمله بالصوت المحدد).

- **ترتيب المرجع الصوتي**: عند استخدام وضع "مرجع صوتي"، يجب توصيل مدخلات الصوت المرجعي بالتسلسل بدءاً من `reference_audio_1` دون فجوات. على سبيل المثال، يمكنك توصيل _1 و _2، ولكن ليس _1 و _3 دون _2.

- **الحد الأقصى لوسوم الصوت**: يمكن أن يشير الموجه إلى ما يصل إلى 3 مقاطع صوتية (@Audio1، @Audio2، @Audio3) عند استخدام وضع "مرجع صوتي". يجب ألا يتجاوز الوسم ذو الرقم الأعلى عدد مدخلات الصوت المرجعي المتصلة.

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
|-------------|-------|---------------|
| `AUDIO` | المخرج الصوتي المُولَّد من ByteDance Seed Audio 1.0، يحتوي على الكلام أو الموسيقى أو المؤثرات الصوتية أو الحوار متعدد المتحدثين كما هو موصوف في الموجه. | AUDIO |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ByteDanceSeedAudio/ar.md)

---
**Source fingerprint (SHA-256):** `cefd5fca496b02c35022d25be3d99d3911c1304b6e3a751751b58841d5895ef7`
