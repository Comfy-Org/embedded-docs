# ByteDanceSeedAudio

### نمای کلی

با استفاده از Seed Audio 1.0 شرکت ByteDance، از یک پرامپت واحد، گفتار، موسیقی، افکت‌های صوتی و دیالوگ چند‌گوینده تولید کنید. صدا(ها)، احساسات، فضای محیط، موسیقی پس‌زمینه و افکت‌های صوتی را در پرامپت توصیف کنید و متن‌های گفتاری را نیز در آن بگنجانید. به‌صورت اختیاری، می‌توانید یک صدای از پیش‌تعیین‌شده داخلی انتخاب کنید، صداها را از حداکثر ۳ کلیپ مرجع (که در پرامپت با @Audio1-3 برچسب‌گذاری می‌شوند) شبیه‌سازی کنید، یا صدایی را از روی تصویر یک شخصیت استخراج کنید. در هر اجرا حداکثر ۲ دقیقه صوت تولید می‌شود.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
|---------|---------|----------|--------|--------|
| `text_prompt` | صدا(ها)، احساسات، سرعت، فضای محیط، موسیقی پس‌زمینه و افکت‌های صوتی را توصیف کنید و متن‌های گفتاری را نیز در آن بگنجانید (برای دیالوگ، نام شخصیت‌ها را درون متن ذکر کنید). در حالت «مرجع صوتی»، با ترتیب به کلیپ‌های متصل با @Audio1، @Audio2، @Audio3 ارجاع دهید. با مدل چندزبانه، یک خط نقل‌قول‌دار می‌تواند با محدوده مهر زمانی شروع شود که زمان و مدت بیان آن را کنترل می‌کند، مثلاً `[5.5s:8.0s] منتظر من بمان!`. پرامپت را به همان زبانی بنویسید که خطوط باید بیان شوند. حداقل ۱ کاراکتر، حداکثر ۳۰۰۰ کاراکتر. | STRING | بله | ۱ تا ۳۰۰۰ کاراکتر |
  - **"مرجع صوتی"**: نیاز به اتصال حداقل یکی از `reference_audio_1`، `reference_audio_2` یا `reference_audio_3` دارد. کلیپ‌های مرجع باید به ترتیب و بدون شکاف متصل شوند. هر کلیپ حداکثر ۳۰ ثانیه محدود است. اگر از برچسب‌های @AudioN در پرامپت استفاده شود، بالاترین شماره برچسب نباید از تعداد کلیپ‌های مرجع متصل تجاوز کند.
| `reference_audio_1` | کلیپ مرجع برای شبیه‌سازی صدا که در پرامپت با @Audio1 برچسب‌گذاری می‌شود. حداکثر ۳۰ ثانیه. فقط زمانی در دسترس است که `reference_mode` برابر با "audio reference" باشد. | AUDIO | خیر | حداکثر ۳۰ ثانیه |
| `reference_audio_2` | کلیپ مرجع که در پرامپت با @Audio2 برچسب‌گذاری می‌شود. حداکثر ۳۰ ثانیه. فقط زمانی در دسترس است که `reference_mode` برابر با "audio reference" باشد. | AUDIO | خیر | حداکثر ۳۰ ثانیه |
| `reference_audio_3` | کلیپ مرجع که در پرامپت با @Audio3 برچسب‌گذاری می‌شود. حداکثر ۳۰ ثانیه. فقط زمانی در دسترس است که `reference_mode` برابر با "audio reference" باشد. | AUDIO | خیر | حداکثر ۳۰ ثانیه |
  - **"مرجع تصویری"**: نیاز به اتصال `reference_image` دارد. برچسب‌های @AudioN استفاده نمی‌شوند؛ پرامپت باید فقط متن مورد نظر برای سنتز را شامل شود.
  - **"صدای از پیش‌تعیین‌شده"**: نیاز به انتخاب یک صدای از پیش تعیین‌شده دارد. کل پرامپت با صدای انتخاب‌شده خوانده می‌شود؛ برچسب‌های @AudioN به‌عنوان مرجع استفاده نمی‌شوند و برچسب‌هایی مانند @Audio2 یا بالاتر رد می‌شوند.
| `sample_rate` | نرخ نمونه‌برداری خروجی بر حسب هرتز. (پیش‌فرض: "24000") | COMBO | بله | `"8000"`<br>`"16000"`<br>`"24000"`<br>`"32000"`<br>`"44100"`<br>`"48000"` |
| `speech_rate` | سرعت گفتار. ۰ = عادی، ۱۰۰ = ۲.۰ برابر، ۵۰- = ۰.۵ برابر. (پیش‌فرض: ۰) | INT | بله | ۵۰- تا ۱۰۰ |
| `loudness_rate` | بلندی صدا. ۰ = عادی، ۱۰۰ = ۲.۰ برابر، ۵۰- = ۰.۵ برابر. (پیش‌فرض: ۰) | INT | بله | ۵۰- تا ۱۰۰ |
| `pitch_rate` | تغییر زیروبمی صدا بر حسب نیم‌پرده (۱۲- تا ۱۲). (پیش‌فرض: ۰) | INT | بله | ۱۲- تا ۱۲ |
| `seed` | Seed تعیین می‌کند که آیا گره باید دوباره اجرا شود یا خیر؛ نتایج صرف‌نظر از seed غیرقطعی هستند. (پیش‌فرض: ۴۲) | INT | بله | ۰ تا ۲۱۴۷۴۸۳۶۴۷ |
| `model` | نسخه مدل. `seed-audio-1.0-multilingual` از ۲۰ زبان و کنترل زمان‌بندی جمله‌به‌جمله از طریق مهرهای زمانی `[5.5s:8.0s]` پشتیبانی می‌کند. `seed-audio-1.0` فقط از انگلیسی و چینی پشتیبانی می‌کند، بدون کنترل زمان‌بندی. (پیش‌فرض: "seed-audio-1.0-multilingual") | COMBO | خیر | `"seed-audio-1.0-multilingual"`<br>`"seed-audio-1.0"` |

### محدودیت‌های پارامترها

- **وابستگی‌های حالت مرجع**: پارامتر `reference_mode` تعیین می‌کند که کدام ورودی‌های دیگر مورد نیاز هستند:
  - **"فقط متن"**: هیچ ورودی اضافی مورد نیاز نیست. پرامپت نباید حاوی برچسب‌های @AudioN باشد.
  - **"مرجع صوتی"**: نیاز به اتصال حداقل یکی از `reference_audio_1`، `reference_audio_2` یا `reference_audio_3` دارد. کلیپ‌های مرجع باید به ترتیب و بدون شکاف متصل شوند (مثلاً _1، سپس _2، سپس _3). هر کلیپ حداکثر ۳۰ ثانیه طول می‌کشد. پرامپت باید با استفاده از برچسب‌های @Audio1، @Audio2، @Audio3 به کلیپ‌های متصل ارجاع دهد.
  - **"مرجع تصویری"**: نیاز به اتصال `reference_image` دارد. پرامپت نباید حاوی برچسب‌های @AudioN باشد.
  - **"صدای از پیش‌تعیین‌شده"**: نیاز به انتخاب `preset_voice` دارد. پرامپت نباید حاوی برچسب‌های @AudioN باشد (کل پرامپت با صدای انتخاب‌شده خوانده می‌شود).

- **ترتیب مرجع صوتی**: هنگام استفاده از حالت "audio reference"، ورودی‌های مرجع صوتی باید به‌صورت متوالی و از `reference_audio_1` بدون شکاف متصل شوند. به‌عنوان مثال، می‌توانید _1 و _2 را متصل کنید، اما نمی‌توانید _1 و _3 را بدون _2 متصل کنید.

- **حداکثر برچسب‌های صوتی**: در حالت "audio reference"، پرامپت می‌تواند به حداکثر ۳ کلیپ صوتی (@Audio1، @Audio2، @Audio3) ارجاع دهد. بالاترین شماره برچسب نباید از تعداد ورودی‌های مرجع صوتی متصل بیشتر باشد.

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
|-----------|---------|----------|
| `AUDIO` | خروجی صوتی تولیدشده توسط ByteDance Seed Audio 1.0، شامل گفتار، موسیقی، افکت‌های صوتی یا دیالوگ چند‌گوینده مطابق با توضیحات پرامپت. | AUDIO |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ByteDanceSeedAudio/fa.md)

---
**Source fingerprint (SHA-256):** `cefd5fca496b02c35022d25be3d99d3911c1304b6e3a751751b58841d5895ef7`
