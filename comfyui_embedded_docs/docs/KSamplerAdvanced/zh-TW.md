> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/KSamplerAdvanced/zh-TW.md)

KSamplerAdvanced 節點旨在透過提供進階配置與技術來增強取樣過程。其目標是提供更精密的選項，以便從模型中生成樣本，並改進基本 KSampler 的功能。

## 輸入

| 參數 | 資料類型 | 描述 |
|----------------------|-------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `model` | MODEL | 指定要從中生成樣本的模型，在取樣過程中扮演關鍵角色。 |
| `add_noise` | COMBO[STRING] | 決定是否應在取樣過程中添加雜訊，影響生成樣本的多樣性與品質。 |
| `noise_seed` | INT | 設定雜訊生成的種子，確保取樣過程的可重現性。 |
| `steps` | INT | 定義取樣過程中要執行的步驟數，影響輸出的細節與品質。 |
| `cfg` | FLOAT | 控制條件因子，影響取樣過程的方向與空間。 |
| `sampler_name` | COMBO[STRING] | 選擇要使用的特定取樣器，允許自訂取樣技術。 |
| `scheduler` | COMBO[STRING] | 選擇用於控制取樣過程的排程器，影響樣本的進展與品質。 |
| `positive` | CONDITIONING | 指定正向條件引導，以引導取樣朝向所需的屬性。 |
| `negative` | CONDITIONING | 指定負向條件引導，以引導取樣遠離某些屬性。 |
| `latent_image` | LATENT | 提供取樣過程中使用的初始潛在影像，作為起始點。 |
| `start_at_step` | INT | 決定取樣過程的起始步驟，允許控制取樣進程。 |
| `end_at_step` | INT | 設定取樣過程的結束步驟，定義取樣的範圍。 |
| `return_with_leftover_noise` | COMBO[STRING] | 指示是否返回帶有殘留雜訊的樣本，影響最終輸出的外觀。 |

## 輸出

| 參數 | 資料類型 | 描述 |
|-------------|-------------|------------------------------------------------------------------------------------------------------------------------------|
| `latent` | LATENT | 輸出代表從模型生成的潛在影像，反映了所應用的配置與技術。 |