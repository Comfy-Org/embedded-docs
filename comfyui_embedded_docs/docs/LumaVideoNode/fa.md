> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LumaVideoNode/fa.md)

## ورودی‌ها

| پارامتر | نوع داده | الزامی | محدوده | توضیحات |
|---------|----------|--------|--------|----------|
| `prompt` | STRING | بله | - | متن راهنما برای تولید ویدیو (پیش‌فرض: رشته خالی). باید حداقل ۳ کاراکتر داشته باشد. |
| `model` | COMBO | بله | `"ray_1_6"`<br>`"ray_2"` | مدل تولید ویدیوی مورد استفاده. |
| `aspect_ratio` | COMBO | بله | `"16:9"`<br>`"9:16"`<br>`"1:1"`<br>`"4:3"`<br>`"3:4"`<br>`"21:9"`<br>`"9:21"` | نسبت ابعاد ویدیوی تولیدشده (پیش‌فرض: "16:9"). |
| `resolution` | COMBO | بله | `"540p"`<br>`"720p"`<br>`"1080p"` | رزولوشن خروجی ویدیو (پیش‌فرض: "540p"). این پارامتر هنگام استفاده از مدل `ray_1_6` نادیده گرفته می‌شود. |
| `duration` | COMBO | بله | `"5s"`<br>`"9s"` | مدت زمان ویدیوی تولیدشده. این پارامتر هنگام استفاده از مدل `ray_1_6` نادیده گرفته می‌شود. |
| `loop` | BOOLEAN | بله | - | آیا ویدیو باید حلقه‌وار تکرار شود (پیش‌فرض: False). |
| `seed` | INT | بله | ۰ تا ۱۸۴۴۶۷۴۴۰۷۳۷۰۹۵۵۱۶۱۵ | دانه تصادفی برای تعیین اجرای مجدد گره؛ نتایج واقعی بدون توجه به seed غیرقطعی هستند (پیش‌فرض: ۰). |
| `luma_concepts` | CUSTOM | خیر | - | مفاهیم دوربین اختیاری برای تعیین حرکت دوربین از طریق گره Luma Concepts. |

**توجه:** هنگام استفاده از مدل `ray_1_6`، پارامترهای `duration` و `resolution` به‌طور خودکار نادیده گرفته می‌شوند و تأثیری بر تولید ندارند.

## خروجی‌ها

| نام خروجی | نوع داده | توضیحات |
|-----------|----------|---------|
| `output` | VIDEO | فایل ویدیوی تولیدشده. |

---
**Source fingerprint (SHA-256):** `44482bc91c3df2cc9ac22d06197668af45849e8bfde8bd435905f11f2593342c`
