> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [Edit on GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/SamplerDpmpp2mSde/zh-TW.md)

此節點專為 DPMPP_2M_SDE 模型生成取樣器而設計，可根據指定的求解器類型、噪聲級別和計算設備偏好來創建樣本。它抽象了取樣器配置的複雜性，提供了一個簡化的界面，用於生成具有自定義設置的樣本。

## 輸入參數

| 參數名稱         | 資料類型       | 描述                                                                 |
|------------------|----------------|----------------------------------------------------------------------|
| `solver_type`    | COMBO[STRING]  | 指定取樣過程中使用的求解器類型，可選擇 'midpoint' 或 'heun'。此選擇會影響取樣過程中應用的數值積分方法。 |
| `eta`            | `FLOAT`        | 決定數值積分中的步長大小，影響取樣過程的細粒度程度。數值越高表示步長越大。 |
| `s_noise`        | `FLOAT`        | 控制取樣過程中引入的噪聲水平，影響生成樣本的變異性。 |
| `noise_device`   | COMBO[STRING]  | 指定執行噪聲生成過程的計算設備（'gpu' 或 'cpu'），會影響效能和效率。 |

## 輸出參數

| 參數名稱         | 資料類型       | 描述                                                                 |
|------------------|----------------|----------------------------------------------------------------------|
| `sampler`        | `SAMPLER`      | 輸出是根據指定參數配置的取樣器，已準備好用於生成樣本。 |