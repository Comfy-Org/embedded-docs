# WanMoveConcatTrack

گره WanMoveConcatTrack دو مجموعه داده ردیابی حرکت را در یک توالی واحد و طولانی‌تر ترکیب می‌کند. این کار با اتصال مسیرهای ردیابی و ماسک‌های دید از ورودی‌ها در امتداد ابعاد مربوطه انجام می‌شود. اگر فقط یک ورودی ردیابی ارائه شود، همان داده را بدون تغییر عبور می‌دهد.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
| --- | --- | --- | --- | --- |
| `tracks_1` | اولین مجموعه داده ردیابی حرکت که باید به هم متصل شود. | TRACKS | بله |  |
| `tracks_2` | مجموعه دوم اختیاری داده ردیابی حرکت. اگر ارائه نشود، `tracks_1` مستقیماً به خروجی منتقل می‌شود. | TRACKS | خیر |  |

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
| --- | --- | --- |
| `tracks` | داده ردیابی حرکت به‌هم‌متصل‌شده، شامل `track_path` و `track_visibility` ترکیبی از ورودی‌ها. | TRACKS |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/WanMoveConcatTrack/fa.md)

---
**Source fingerprint (SHA-256):** `0507c42dce5d481fe5dc5aa1116c9df279f236419f548ea3eff5d824d0d22653`
